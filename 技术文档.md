# quantitative_analysis 技术文档

> 面向本项目的完整技术说明书：**功能实现原理、数据源、接口**。
> 生成方式：通读全部源码（`app/` 137 个 .py + `frontend/src`）后按层梳理，所有结论均标注了文件路径。
> 项目：Flask + SQLAlchemy(SQLite) + SocketIO 的 A 股多因子量化选股系统；数据主存为 **Parquet**，SQLite 只存应用状态。

---

## 目录

1. [项目概览](#1-项目概览)
2. [架构总览](#2-架构总览)
3. [启动与装配层](#3-启动与装配层)
4. [存储契约](#4-存储契约)
5. [数据源适配层](#5-数据源适配层)
6. [数据下载任务（data_jobs）](#6-数据下载任务data_jobs)
7. [服务层实现原理](#7-服务层实现原理)
8. [HTTP 接口全清单](#8-http-接口全清单)
9. [WebSocket](#9-websocket)
10. [前端](#10-前端)
11. [关键口径与设计约束（必读）](#11-关键口径与设计约束必读)
12. [数据源现状与缺口](#12-数据源现状与缺口)

---

## 1. 项目概览

| 项 | 内容 |
|---|---|
| 定位 | A 股多因子量化选股系统：数据下载 → 因子计算 → 评分选股 → 组合优化 → 回测验证 → 实时监控 |
| 后端 | Flask 3.1 + Flask-SQLAlchemy 3.1 + Flask-SocketIO + Flask-CORS |
| 前端 | 双套：① 后端 Jinja 模板（`app/templates/`，服务端渲染）；② 独立 Vite 前端（`frontend/`，React 19 + Vite 7 + TS 5.8 + Tailwind 3.4，端口 5173） |
| 行情存储 | **Parquet**（Hive 分区 `year=/month=/day=/data.parquet`），根目录 `DATA_DIR`（默认 `data/`） |
| 应用状态 | **SQLite**（`instance/stock_cursor.sqlite3`，WAL + `synchronous=NORMAL` + `timeout=30`） |
| 任务执行 | **进程内**（去 Redis/Celery）：data_jobs 走后台守护线程 + 子进程；回测走 `threading.Thread`；模型训练走 `ThreadPoolExecutor(2)` |
| Python | 3.8–3.11 官方支持（本项目实测 3.12.7 + `runtime_compat.py` 补丁可运行） |
| 入口 | `run.py`（唯一 Web 入口，5000）；`run_system.py`（初始化/诊断菜单） |
| 测试 | `pytest`；`tests/{services,api,regression,ui}`；重依赖在 `tests/conftest.py` 打桩 |

---

## 2. 架构总览

### 2.1 请求流

```
HTTP  → Blueprint (app/api/*.py)      → Service (app/services/*.py) → ParquetDataReader / SQLAlchemy(SQLite) / ParquetStateStore
页面  → Blueprint (app/routes/*.py, app/main/*.py) → Jinja 模板
WS    → app/websocket/websocket_events.py → services/websocket_push_service.py → socketio 房间广播
SSE   → /api/ai-assistant/chat → services/ai/assistant_service.py
```

### 2.2 分层

```
┌── 前端 ──────────────────────────────────────────────┐
│  frontend/ (React+Vite :5173)  │  app/templates/ (Jinja :5000)
└──────────────┬───────────────────────┬───────────────┘
        /api/* │               页面路由 │
┌──────────────▼───────────────────────▼───────────────┐
│  API 层  app/api/*（12 个蓝图）  +  app/routes/*（页面）│
├──────────────────────────────────────────────────────┤
│  服务层  app/services/*（40+ 模块）                    │
│   因子/评分/组合/回测 │ ML │ Text2SQL │ 实时族 │ 市场族 │
├──────────────────────────────────────────────────────┤
│  数据访问层                                            │
│   ParquetDataReader │ MinuteParquetReader             │
│   ParquetStateStore │ ParquetEventStore │ DataJobStore│
├──────────────────────────────────────────────────────┤
│  存储  Parquet(DATA_DIR)  │  SQLite(instance/*.db)     │
├──────────────────────────────────────────────────────┤
│  数据源  fuyao(扶摇) / tickflow / 通达信数仓 / tushare  │
│          + 本地自算(derived)                          │
└──────────────────────────────────────────────────────┘
```

### 2.3 三套持久层包装

| 包装类 | 文件 | 承载 |
|---|---|---|
| `ParquetStateStore` | `services/parquet_state_store.py` | ml-factor 业务对象：因子定义/值、模型定义/预测、组合持仓、回测 run/结果 |
| `ParquetEventStore` | `services/parquet_event_store.py` | 实时事件：`indicators`、`signals`（按天分区 append/query） |
| `ParquetDataJobStateStore` | `services/data_jobs/parquet_state_store.py` | 数据任务运行记录 `data_job_runs` / 游标 `data_job_cursors` |

---

## 3. 启动与装配层

### 3.1 `run.py`

| 环节 | 说明 |
|---|---|
| UTF-8 保护 | 导入 app 前对 `sys.stdout/stderr` 调 `reconfigure(encoding='utf-8', errors='replace')`（防 GBK 控制台打印 ⚠/中文崩） |
| eventlet | 仅当 `SOCKETIO_ASYNC_MODE=eventlet` 时先 `monkey_patch()` 再导入应用 |
| 健康检查 | `inspect_parquet_data_assets()` → `build_health_report()` |
| 启动输出 | `build_startup_report()` + `build_health_summary_lines()` + 大宽表状态 |
| 服务 | `socketio.run(app, host='0.0.0.0', port=int(os.getenv('PORT',5000)), use_reloader=False, allow_unsafe_werkzeug=True)` |

### 3.2 `run_system.py`

菜单式诊断：`1` 检查依赖 → `2` 初始化数据库（`db.create_all()` + 创建 **12 个内置因子**）→ `3/4` 启动 Web → `5` 演示 → `6` 系统信息。

依赖检查清单：`flask, sqlalchemy, pandas, numpy, sklearn, xgboost, lightgbm, cvxpy, loguru, requests`。
> ⚠️ 原代码用 `__import__('scikit-learn')`（pip 包名当模块名）会误报缺失，已修正为 `'sklearn'`。

12 个内置因子：`momentum_1d/5d/20d`、`volatility_20d`、`rsi_14`、`turnover_rate`、`pe_ratio`、`pb_ratio`、`roe`、`debt_ratio`、`current_ratio`、`gross_margin`。

### 3.3 `config.py`

`load_dotenv()` 后从环境变量读取。关键项：

| 配置 | 环境变量 | 默认 |
|---|---|---|
| `SQLITE_DATABASE_PATH` | 同名 | `BASE_DIR/stock_cursor.sqlite3` |
| `SECRET_KEY` | 同名 | `''` |
| `DEFAULT_STAMP_DUTY_RATE` | 同名 | `0.0005`（印花税，卖出单边） |
| `RISK_FREE_RATE` | 同名 | `0.03` |
| `CORS_ORIGINS` | 同名 | `*` |
| `SOCKETIO_ASYNC_MODE` | 同名 | `threading` |
| `LOG_LEVEL` / `LOG_FILE` | 同名 | `INFO` / `logs/stock_analysis.log` |
| `DATA_DIR` | 同名 | `BASE_DIR/data` |
| `DATA_UPDATE_HOUR/MINUTE` | 同名 | `18` / `0` |
| `LLM_CONFIG` | `LLM_PROVIDER/…` | ollama / `qwen2.5-coder:latest`（Text2SQL 用） |
| `AI_ASSISTANT_CONFIG` | `LLM_API_KEY/…` | DeepSeek `deepseek-chat`（AI 工作台用） |

三套子配置：`DevelopmentConfig`(DEBUG=True) / `ProductionConfig`(DEBUG=False，**强制校验 SECRET_KEY**，为示例值时 `RuntimeError`) / `TestingConfig`。

> ⚠️ SQLite 相对路径陷阱：`SQLALCHEMY_DATABASE_URI` 若为相对路径，Flask-SQLAlchemy 3.x 会拼到 `app.instance_path` 之下（`./instance/x.db` → `instance/instance/x.db`）。本项目 `.env` 已改用**绝对路径**。

### 3.4 `startup_runtime.py`

**必需数据资产 `PARQUET_ASSETS`**（健康检查判定依据）：
```
stock_basic.parquet, stock_trade_calendar.parquet,
daily_history/daily, daily_basic/daily, stock_business.parquet
```
`build_health_report()` 产出 `{entrypoint, database:{ok,connected,missing_tables,empty_tables,next_actions}, data_jobs:{execution_mode}}`。

### 3.5 `create_app()`（`app/__init__.py`）

顺序：`ensure_click_parameter_source()` → 校验 config 名 → `Flask(__name__)` + `from_object` → Production `validate()` → `db.init_app` → `socketio.init_app` → `CORS` → `setup_logger` → 注册蓝图 → `import app.websocket.websocket_events`（导入即注册事件）。

**蓝图注册表**

| 蓝图 | 文件 | url_prefix |
|---|---|---|
| `api_bp` | `api/__init__.py` | `/api` |
| `ml_factor_bp` | `api/ml_factor_api.py` | `/api/ml-factor` |
| `text2sql_bp` | `api/text2sql_api.py` | `/api/text2sql` |
| `realtime_analysis_bp` | `api/realtime_analysis.py` | `/api/realtime-analysis` |
| `realtime_indicators_bp` | `api/realtime_indicators.py` | `/api/realtime-analysis/indicators` |
| `realtime_signals_bp` | `api/realtime_signals.py` | `/api/realtime-analysis/signals` |
| `realtime_monitor_bp` | `api/realtime_monitor.py` | `/api/realtime-analysis/monitor` |
| `realtime_risk_bp` | `api/realtime_risk.py` | `/api/realtime-analysis/risk` |
| `realtime_report_bp` | `api/realtime_report.py` | `/api/realtime-analysis/reports` |
| `websocket_api_bp` | `api/websocket_api.py` | `/api/websocket` |
| `data_jobs_bp` | `api/data_jobs_api.py` | `/api/data-jobs` |
| `ai_assistant_bp` | `api/ai_assistant_api.py` | `/api/ai-assistant` |
| `market_bp` / `datasources_bp` | `api/market_api.py` | `/api/market` / `/api/datasources` |
| `pattern_screen_api` | `api/pattern_screen_api.py` | `/api/pattern-screen` |
| `ml_factor_routes` | `routes/ml_factor_routes.py` | `/ml-factor`（页面） |
| `realtime_analysis_routes` | `routes/realtime_analysis_routes.py` | `/realtime-analysis`（页面） |
| `heatmap_routes` | `routes/heatmap.py` | `/heatmap`（页面） |
| `pattern_screen_bp` | `routes/pattern_screen.py` | `/pattern-screen/`（页面） |
| `main_bp` | `main/__init__.py` | 无前缀（首页/股票/分析/数据管理等页面） |

### 3.6 `app/extensions.py`

```python
@event.listens_for(Engine, "connect")   # SQLite: PRAGMA journal_mode=WAL + synchronous=NORMAL
db = SQLAlchemy()
socketio = SocketIO(cors_allowed_origins="*", async_mode=os.getenv('SOCKETIO_ASYNC_MODE','threading'))
```

### 3.7 `app/celery_app.py`（假 Celery）

`_LocalTaskWrapper` 使 `@celery.task` 只注册、`.delay()` **等价同步调用**，保持 `app.celery_app.celery` 模块路径不变。末尾导入 `app.tasks.{data_jobs_tasks,report_tasks,backtest_tasks}` 完成注册。

### 3.8 `runtime_compat.py`

`ensure_click_parameter_source()`：老版 Click 缺 `click.core.ParameterSource` 时动态补枚举类，幂等。

---

## 4. 存储契约

### 4.1 Parquet 目录布局（`DATA_DIR`）

| 路径 | 分区键 | 生产者 |
|---|---|---|
| `daily_history/daily/year=/month=/day=/data.parquet` | `trade_date` | tushare / **fuyao** |
| `daily_basic/daily/...` | `trade_date` | tushare |
| `stk_factor/daily/...` | `trade_date` | tushare / **derived（本地自算）** |
| `moneyflow/daily/...`、`moneyflow_ths/daily/...` | `trade_date` | tushare |
| `cyq_perf/daily/...` | `trade_date` | tushare |
| `income_statement/`、`balance_sheet/`、`cash_flow/`（`year=/month=/day=`） | `end_date`（报告期） | tushare VIP / **fuyao** |
| `stock_basic.parquet` | — | tushare（权威）/ fuyao（清单） |
| `stock_trade_calendar.parquet` | — | tushare / **fuyao `trading_days()` 生成** |
| `stock_company.parquet` | — | tushare |
| `stock_business.parquet` | — | `wide_table_builder.py`（大宽表，最新日） |
| `stock_ma_data.parquet` | — | `ma_calculator.py` |
| `index_basic.parquet`、`index_daily/stock/ts_code=XXX/data.parquet` | — / `ts_code` | reader 读取 |
| `stock_minute/{period_type}/year=/month=/day=/data.parquet` | 周期+日期 | 通达信/Baostock 同步服务 |
| `data_job_state/data_job_runs.parquet`、`data_job_cursors.parquet` | — | data_jobs 状态库 |
| `ml_factor_state/`（`factor_definitions/factor_values/**`） | 部分按 `trade_date` | `ParquetStateStore` |
| `realtime_events/{indicators,signals}/year=/month=/day=/data.parquet` | 日期 | `ParquetEventStore` |
| `cache/fuyao/daily_k__*.parquet`、`daily_k_10d__*.parquet` | — | fuyao dump 缓存 |
| `text2sql_query.db` | — | text2sql 隔离只读查询库（**物理隔离应用主库**） |

### 4.2 主要表 schema

**`daily_history/daily`**（`DAILY_COLUMNS`）
`ts_code, trade_date, open, high, low, close, pre_close, change, pct_chg, vol(手), amount(千元)`

**`daily_basic/daily`**
`ts_code, trade_date, close, turnover_rate, turnover_rate_f, volume_ratio, pe, pe_ttm, pb, ps, ps_ttm, dv_ratio, dv_ttm, total_share, float_share, free_share, total_mv, circ_mv`

**`stk_factor/daily`**（35 列）
`ts_code, trade_date, close, open, high, low, pre_close, change, pct_change, vol, amount, adj_factor, open_hfq, open_qfq, close_hfq, close_qfq, high_hfq, high_qfq, low_hfq, low_qfq, pre_close_hfq, pre_close_qfq, macd_dif, macd_dea, macd, kdj_k, kdj_d, kdj_j, rsi_6, rsi_12, rsi_24, boll_upper, boll_mid, boll_lower, cci`

**`moneyflow/daily`**
`ts_code, trade_date, buy_sm_vol, buy_sm_amount, sell_sm_vol, sell_sm_amount, buy_md_vol, buy_md_amount, sell_md_vol, sell_md_amount, buy_lg_vol, buy_lg_amount, sell_lg_vol, sell_lg_amount, buy_elg_vol, buy_elg_amount, sell_elg_vol, sell_elg_amount, net_mf_vol, net_mf_amount`

**`cyq_perf/daily`**
`ts_code, trade_date, his_low, his_high, cost_5pct, cost_15pct, cost_50pct, cost_85pct, cost_95pct, weight_avg, winner_rate`

**`moneyflow_ths/daily`**
`ts_code, trade_date, name, pct_change, latest, net_amount, net_d5_amount, buy_lg_amount, buy_lg_amount_rate, buy_md_amount, buy_md_amount_rate, buy_sm_amount, buy_sm_amount_rate`

**财务三表**：`income_statement`(79 列) / `balance_sheet`(156 列) / `cash_flow`(101 列)，字段清单见 §6.4。

**`stock_basic.parquet`**：`ts_code, symbol, name, area, industry, list_date, delist_date, list_status`（`L/D/P`）

**`stock_trade_calendar.parquet`**：`exchange, cal_date, is_open, pretrade_date`

**`stock_company.parquet`**：`ts_code, exchange, chairman, manager, secretary, reg_capital, setup_date, province`

**`stock_business.parquet`**（大宽表，最新交易日）= `daily_basic` 全列 + `stk_factor` 改名（`factor_open/factor_high/…/factor_cci`）+ `adj_factor` + `moneyflow_net_amount/net_vol` + `stock_name/symbol` + `year/month/day`

**`stock_ma_data.parquet`**：`ts_code, ma5…ma120, ema5…ema120`

### 4.3 SQLite 表（`app/models/`）

| 表 | 类 | 用途 |
|---|---|---|
| `data_job_run` | `DataJobRun` | 任务运行记录（**ORM 镜像**，运行时实际用 Parquet 状态库） |
| `table_metadata` / `field_metadata` | `TableMetadata` / `FieldMetadata` | Text2SQL 表/字段语义元数据 |
| `query_template` / `query_history` / `business_dictionary` | 同名类 | Text2SQL 模板/历史/同义词词典 |
| `ai_chat_session` / `ai_chat_message` | `AiChatSession` / `AiChatMessage` | AI 工作台会话与消息 |
| `portfolio_positions` | `PortfolioPosition` | 持仓（含 `current_price/market_value/unrealized_pnl/weight/var_1d/stop_loss_price/…`） |
| `realtime_indicators` | `RealtimeIndicator` | **表壳**，数据实际走 `ParquetEventStore` |
| `trading_signals` | `TradingSignal` | **表壳**，数据走 `ParquetEventStore` |
| `report_templates` / `realtime_reports` / `report_subscriptions` | 同名类 | 报告模板/报告/订阅 |
| `risk_alerts` | `RiskAlert` | 风险预警 |
| （非 ORM） | `StockMinuteData` | 分钟数据代理类（实际 Parquet） |

### 4.4 `ParquetDataReader`（`services/data_reader.py`）

- `TABLE_DIRS`：`daily→daily_history/daily`、`daily_basic→daily_basic/daily`、`stk_factor→stk_factor/daily`、`moneyflow→moneyflow/daily`、`cyq_perf→cyq_perf/daily`、财务三表→同名目录。
- 读取流程：扫分区 → `pd.read_parquet` → `concat` → `STANDARD_COLUMNS` 白名单裁列 → `ts_code` 过滤 → `trade_date` 用 `format="mixed"` 解析（兼容 `YYYYMMDD` 与 `YYYY-MM-DD`）→ 排序。
- **复权口径 `get_return_prices`**：优先用 `stk_factor.close_hfq`（后复权）；**单只复权覆盖率 ≥0.5 才整体采用**，否则整只退回不复权（避免同序列混口径）。这是因子与 ML 标签的统一口径。
- 单文件表（`stock_basic` 等）用 **mtime 失效缓存**（跨进程安全）。
- 常用方法：`get_daily/get_daily_basic/get_stk_factor/get_return_prices/get_moneyflow/get_cyq_perf/get_income_statement/get_balance_sheet/get_cash_flow/get_stock_basic/get_stock_basic_list/get_industry_list/get_area_list/get_trade_calendar/get_stock_company/get_index_basic/get_stock_business/get_ma_data/get_latest_close/get_latest_daily/get_trade_dates/get_minute_reader`。

---

## 5. 数据源适配层

`app/utils/data_sources/`：三方客户端 + 归一化层，**同一张 Parquet 表可有多个"可替换生产者"，读取侧无感知**。

### 5.1 `fuyao_client.py`（扶摇/同花顺官方）

常量：`BASE_URL=https://fuyao.aicubes.cn`、认证头 `X-api-key`、节流 `DEFAULT_THROTTLE_SECONDS=0.12`、`SNAPSHOT_PAGE_SIZE=6000`、`BATCH_LIMIT=100`、`ANOMALY_BATCH_LIMIT=50`。

**方法清单**

| 方法 | 端点 | 限制 |
|---|---|---|
| `snapshot_page(limit, offset, thscodes)` / `snapshot_all()` | `/api/a-share/prices/snapshot` | thscodes 批量 ≤100；省略 thscodes + limit=10000 可一次拿全市场 |
| `index_snapshot(thscodes)` | `/api/a-share-index/prices/snapshot` | ≤100；**未知代码整批 1002 连坐（.BJ 须先过滤）** |
| `ths_index_catalog(tag)` | `/api/a-share-index/catalog/ths-index-list` | industry 320 / cn_concept 390 |
| `ths_index_constituents(thscode)` | `/api/a-share-index/constituents/ths-stock-list` | — |
| `historical_kline(thscode, start_ms, end_ms, interval, adjust)` | `/api/a-share/prices/historical` | 窗口 ≤10 年；**adjust 锁定 `none`**（前复权有逐日漂移） |
| `financial_statement(table, thscode, limit, period)` | `/api/a-share/financials/{income-statements｜balance-sheets｜cash-flow-statements}` | 单标的 ≤20 期 |
| `valuations_snapshot(thscodes)` | `/api/a-share/valuations/snapshot` | ≤100；返回 `pe_ttm/pe_mrq/pb_mrq/ps_ttm/pcf_ttm` |
| `trading_days()` | `/api/a-share/calendar/trading-days` | **固定近一年** |
| `dragon_tiger_list(board_type, date)` | `.../special-data/dragon-tiger-list` | all/org/hot_money |
| `short_term_benchmark(date)` | `.../auction/short-term-benchmark` | 一年内 |
| `limit_up_pool(date_ms, page, size, sort_field, sort_dir)` | `.../limit-up-pool` | **date_ms 毫秒戳**（传 date 被静默忽略） |
| `limit_down_pool` / `limit_break_pool` | 同名端点 | 同上 |
| `limit_up_ladder()` | `.../limit-up-ladder` | 近 30 交易日连板矩阵 |
| `hot_stock_list(period)` / `skyrocket_list(period)` | `.../hot-stock-list`、`skyrocket-list` | Top30（day/hour） |
| `hot_stock_list_history(date)` / `hot_stock_rank_trend(thscode, start, end)` | 同名 | 一年内 |
| `anomaly_analysis_list(tag_codes)` / `anomaly_analysis_stock(thscodes)` | `.../anomaly-analysis-*` | 按代码 ≤50 |
| `ticker_search(query, ...)` | `/api/meta/tickers/search` | — |
| `dump_download_url(kind)` / `download_dump(kind, dest)` | `/api/dump/market-dumps/{kind}/download-url` | kind: `daily-k`(10年) / `daily-k-10d` / `adjustment-factors`；**预签名下载不得带 X-api-key** |

错误码：`4001` 限频、`1002` 无效代码/非交易日、`1003` 超批量、`1004` 参数错、`3002` 当日数据不可用、`5003` 未披露报告期。

### 5.2 `fuyao_dump.py`（全市场日K 三档取数）

缓存：`{DATA_DIR}/cache/fuyao/{prefix}__{release}.parquet`，release 取自预签名 URL 的 `releases/<date>/`。`DumpStore.ensure_path(reuse_old_if_covers=ymd)` 可复用覆盖目标日的旧大 dump（避免天天下 172MB）。

`FuyaoDailyFetcher.fetch_dates(trade_dates)` 三档降级：

| 档 | 触发 | 逻辑 | 失败 |
|---|---|---|---|
| 1 | 窗口 ≤12 天 | 用 `daily-k-10d` dump 切分 | 返回 `{}` 降级 |
| 2 | 档 1 未覆盖 | 用 10 年 `daily-k` dump；**末端之后用 10d dump 补尾** | 窗口 >10 年 → `FuyaoError("window_too_long")` |
| 3 | 仍未覆盖 | 逐标的 `historical_kline` 兜底 | **未覆盖 >5 天 → `dates_not_covered`**（防烧两小时） |

### 5.3 `fuyao_normalize.py`（fuyao → tushare 口径）

| 目标列(tushare) | fuyao 来源 | 换算 |
|---|---|---|
| `ts_code` | `thscode` | 直传 |
| `trade_date` | `date_ms` | 北京时区 → `YYYYMMDD` |
| `open/high/low/close` | `open_price/high_price/low_price/close_price` | 直传 |
| `vol` | `volume` | **÷100**（股→手） |
| `amount` | `turnover` | **÷1000**（元→千元） |
| `pre_close` / `change` / `pct_chg` | 推导 | 全 dump 范围先推导再过滤（保边界日正确） |

防御：dump 若 `adjusted != "none"` 直接拒绝落库；OHLC 全空的停牌行 drop；快照字段兼容官方/实测两套命名（`high_price|highest_price` 等）。
财务映射：`operating_income→revenue`、`assets_total→total_assets`、`act_cash_flow_net→n_cashflow_act` 等（详见源码映射表），映射外字段原名透传，`null` 保持 NaN **不伪造 0**。

### 5.4 `tickflow_client.py`

`BASE_URL=https://api.tickflow.org`，认证头 `x-api-key`。方法：`klines(symbol, period, count, adjust, start_time, end_time)`、`quotes(symbols)`、`ex_factors(symbols, ...)`、`instruments(exchange, instrument_type, limit)`、`detect_tier()`。

**档位探测**：无 key → `none`；klines 失败 → `none`；`ex_factors` 403 → `free`；均通过 → `paid`。
权限错误：HTTP 403 + `{"code":"NO_XXX_PERMISSION"}`。`instruments` 的 `ext` 含 `listing_date/total_shares/float_shares/limit_up/limit_down`。

### 5.5 本地数仓（`user/数据/tdx_data.duckdb`）

非本项目的 data_jobs 生产者，但可作为**复权因子/总市值**来源：

- `lake_kline`：`trade_date, stock_code, open, high, low, close, volume(股), amount(万元), forward_factor, vol_in_stock, pct_change, is_stale`
  - ⚠️ **`forward_factor` 是"前复权因子"：最新交易日=1.0、越早越小**（实测 600519.SH 2021-08-02 = 0.864284）
  - ⇒ `P_qfq = P × forward_factor`；`P_hfq = P × (forward_factor / 该股最早因子)`
- `lake_gpjy`：`GP01`(股东人数) `GP14`(涨停金额/开板次数) `GP15`(**涨跌停状态/封单金额**) `GP16`(**总市值/万元**) `GP22`(封成比/封流比) `GP24/34`(首停时间/最大封单) `GP25/29/33/36/38/39/40`
- `dim_stock`：`stock_code, stock_name, market, list_date, status, limit_pct`
- ⚠️ **DuckDB 单写锁**：`tdx_db.py` 在跑时，别的进程**只读也打不开**（`IOException: 另一个程序正在使用`）。

---

## 6. 数据下载任务（data_jobs）

### 6.1 执行链

```
POST /api/data-jobs/submit {job_type, params}
  → DataJobService.submit()          # 清僵尸 run → 查重 → create_run(pending) → queued
     → 后台守护线程 run_data_job(run_id)
        → create_app("development")  # 子线程内建 app context
        → 置 running (progress=5)
        → registry.get_job(job_type)
        → ScriptRunner.run_script(script_path, params)
           → subprocess.run(["python", app/utils/xxx.py], cwd=项目根, env=注入后环境, timeout=3600)
        → rc==0 → success(100)；否则 failed(stderr[-1000:])
```

**关键实现**（`services/data_jobs/`）：
- `registry.py`：任务定义中心（`JobDefinition` = job_type/group/script_path/display_name/description/dangerous/dependencies/default_params/recommended_order/source_name/source_mode/supports_incremental）。
- `service.py`：`submit()` 会先 `reap_stale_runs()` 清超时僵尸、`find_active_duplicate()` 拒绝重复（同 job_type+params 且 pending/queued/running）；**inline 模式用守护线程，提交立即返回**。
- `runner.py`：`subprocess.run(["python", script], cwd=project_root, env=merged_env, capture_output=True, text=True, timeout=…)`；超时**不抛异常**，返回 `returncode=124` 让上层正常落失败。
- `app/tasks/data_jobs_tasks.py::run_data_job(run_id)`：任务执行体；`wide_table_builder` 成功时额外清 `ParquetDataReader` 与 text2sql 缓存。

**环境变量注入规则**（`runner.py`，这是写下载脚本的契约）

| 注入键 | 来源 params |
|---|---|
| `DATA_JOB_START_DATE` / `DATA_JOB_END_DATE` / `DATA_JOB_TRADE_DATE` | `start_date` / `end_date` / `trade_date` |
| `DATA_JOB_FULL_REFRESH` | `full_refresh` |
| `DATA_JOB_SOURCE_NAME` / `DATA_JOB_SOURCE_MODE` / `DATA_JOB_SNAPSHOT_TAG` | 同名键 |
| `DATA_JOB_PARAM_<KEY大写>` | `params` 中**其余任意键** |
| `DATA_JOB_TIMEOUT`（默认 3600）/ `DATA_JOB_MAX_GAP_FILL`（默认 60） | 环境自带 |

> ⚠️ **提交时参数必须放在 `params` 里**（`data_jobs_api.py:36` 只取 `payload.get("params", {})`），放顶层会被**静默丢弃**。

**状态机**：`pending → queued → running → success|failed|cancelled`；落盘 `{DATA_DIR}/data_job_state/data_job_runs.parquet`。

### 6.2 任务清单（`registry.py`）

**页面可见（13 个）**

| job_type | 分组 | 脚本 | 源 | 依赖 | 序 |
|---|---|---|---|---|---|
| `trade_calendar` | 基础资料 | `app/utils/trade_calendar.py` | tushare | — | 1 |
| `stock_basic` | 基础资料 | `app/utils/stock_basic.py` | tushare | — | 2 |
| `stock_basic_fuyao` | 基础资料 | `app/utils/stock_basic_fuyao.py` | **fuyao** | — | 2 |
| `stock_company` | 基础资料 | `app/utils/stock_company.py` | tushare | — | 3 |
| `daily_history_by_date` | 日频行情 | `app/utils/daily_history_by_date.py` | tushare | trade_calendar | 4 |
| `daily_history_fuyao` | 日频行情 | `app/utils/daily_history_fuyao.py` | **fuyao** | trade_calendar | 5 |
| `daily_basic` | 日频行情 | `app/utils/daily_basic.py` | tushare | — | 6 |
| `moneyflow` | 资金流 | `app/utils/moneyflow.py` | tushare | — | 7 |
| `stk_factor` | 资金流 | `app/utils/stk_factor.py` | tushare | — | 8 |
| `stk_factor_derived` | 资金流 | `app/utils/stk_factor_derived.py` | **derived** | daily_history_fuyao | 8 |
| `cyq_perf` | 资金流 | `app/utils/cyq_perf.py` | tushare | — | 9 |
| `wide_table_builder` | 衍生计算 | `app/utils/wide_table_builder.py` | derived | daily_basic,stk_factor,moneyflow,stock_basic | 10 |
| `factor_compute` | 衍生计算 | `app/utils/factor_compute.py` | derived | daily_history_by_code,daily_basic,stk_factor,moneyflow,cyq_perf,income_statement,balance_sheet | 11 |

**隐藏任务**：`daily_history_by_code`、`income_statement`、`balance_sheet`、`cash_flow`、`financial_fuyao`、`moneyflow_ths`、`ma_calculator`（`dangerous=True`）。

> 已从注册表**摘除**：`baostock_daily`/`min5`/`min15`/`min30`/`min60`（硬编码 2025 日期、无视 `DATA_JOB_*`、产物无读取方）。

### 6.3 `parquet_job_helpers.py`（脚本基类与交易日解析）

**`DailyFetchJob`** 契约：子类声明 `job_name` / `rel_table` / `rate_limit_seconds`(0.12) / `max_retries`(3)，实现 `fetch_one(trade_date) -> DataFrame`；`run()` 循环：`resolve_trade_dates_with_gap_fill(rel_table)` → 逐日 `fetch_one`（指数退避重试）→ `save_partitioned_parquet(df, "trade_date", rel_table)`；**重试后仍失败的交易日 `sys.exit(1)`**（缺口下轮补）。

**交易日解析**

| 函数 | 行为 |
|---|---|
| `resolve_trade_dates()` | 有 `DATA_JOB_TRADE_DATE` 只返该日；否则用日历窗口（`END` 缺省=最新交易日） |
| `resolve_trade_dates_with_gap_fill(rel_table)` | 显式参数时同上；**缺省模式对 `[最早分区, 最新交易日]` 做差集回补缺口**，最早分区之前不回补（历史基线）；单次上限 `DATA_JOB_MAX_GAP_FILL=60`，超限从旧往新截断 |
| `available_open_trade_dates()` | 读 `stock_trade_calendar.parquet` 的 `is_open==1` |
| `latest_partition_date()` / `existing_partition_dates()` | 扫 Hive `day=*` 分区 |

**`parquet_writer.py`**

| 函数 | 行为 |
|---|---|
| `save_to_parquet(df, trade_date, table, data_dir)` | **单日整分区覆盖** `{table}/year=/month=/day=/data.parquet` |
| `save_single_parquet(df, filename, data_dir)` | 单文件原子写 |
| `save_partitioned_parquet(df, date_col, table, data_dir)` | 按日期列分组逐日写 |
| `atomic_write_parquet(df, path)` | `.tmp.{pid}` → `os.replace` 原子替换 |
| `parquet_file_lock(lock_path)` | `fcntl.flock` 跨进程互斥（Windows 退化为无操作；**禁同线程重入**） |
| `quarantine_corrupt_parquet(path, log)` | 坏文件改名隔离（避免被当空表覆盖清库） |

### 6.4 各脚本 FIELDS

| 脚本 | 目标 | FIELDS 摘要 |
|---|---|---|
| `stock_basic.py` | `stock_basic.parquet` | `ts_code,symbol,name,area,industry,list_date,delist_date,list_status`（分 L/D/P 三次下载，**为消除幸存者偏差**） |
| `trade_calendar.py` | `stock_trade_calendar.parquet` | `exchange,cal_date,is_open,pretrade_date`；窗口 `20050101~20261231` |
| `stock_company.py` | `stock_company.parquet` | `ts_code,exchange,chairman,manager,secretary,reg_capital,setup_date,province`（SSE/SZSE/BSE 分次） |
| `daily_basic.py` | `daily_basic/daily` | close, turnover_rate(_f), volume_ratio, pe, pe_ttm, pb, ps, ps_ttm, dv_ratio, dv_ttm, total_share, float_share, free_share, total_mv, circ_mv |
| `moneyflow.py` | `moneyflow/daily` | buy/sell × {sm,md,lg,elg} × {vol,amount} + net_mf_vol/amount |
| `moneyflow_ths.py` | `moneyflow_ths/daily` | name, pct_change, latest, net_amount, net_d5_amount, buy_{lg,md,sm}_amount(_rate) |
| `stk_factor.py` | `stk_factor/daily` | 35 列（OHLC + 复权价 + MACD/KDJ/RSI/BOLL/CCI） |
| `cyq_perf.py` | `cyq_perf/daily` | his_low, his_high, cost_5/15/50/85/95pct, weight_avg, winner_rate |
| `income_statement.py` | `income_statement` | 79 列（tushare VIP） |
| `balance_sheet.py` | `balance_sheet` | 156 列（tushare VIP） |
| `cash_flow.py` | `cash_flow` | 101 列（tushare VIP） |
| `daily_history_{by_code,by_date}.py` | `daily_history/daily` | `pro.daily` 默认列 |

**非 tushare 脚本（可用）**

| 脚本 | 说明 |
|---|---|
| `daily_history_fuyao.py` | `FuyaoDailyFetcher.fetch_dates()` → 逐日 `save_to_parquet`；未取到数据的交易日 `return 1` |
| `stock_basic_fuyao.py` | `snapshot_all()` → `snapshot_rows_to_stock_basic(rows, existing)`（**已有记录整体保留**，新代码追加 `list_status=L`）→ `save_single_parquet` |
| `financial_fuyao.py` | 披露截止日推导 `DISCLOSURE_DEADLINES={0331→(4,30),0630→(8,31),0930→(10,31),1231→次年(4,30)}`；增量逐标的拉最近 N 期（≤20）；按 `(ts_code,end_date)` 去重**保留最新 `ann_date`**；`SYMBOL_CHUNK_SIZE=500` 分块落盘 |
| `stk_factor_derived.py` | 见 §7.2（本地自算 35 列） |
| `wide_table_builder.py` | 拼大宽表 `stock_business.parquet` |
| `ma_calculator.py` | 均线衍生 `stock_ma_data.parquet` |
| `factor_compute.py` | 调 `FactorEngine` 算因子 → `factor_values` |

---

## 7. 服务层实现原理

### 7.1 因子层

**`factor_expression_engine.py`（安全表达式引擎）**
用 `ast` 解析而非 `eval`，只放行**时间因果**原语：
- `allowed_columns = {open,high,low,close,pre_close,change_c,pct_chg,vol,amount}`
- `allowed_series_methods = {pct_change, shift, diff, rolling}`（**`rank` 已移出**——全样本前视）
- `causal_period_methods = {pct_change, shift, diff}`：**负 periods 直接抛错**（位置参数与 `periods=` 都拦）
- `allowed_window_methods = {mean,std,max,min,sum}`；`allowed_functions = {abs}`；`max_rolling_window = 10000`
- `extract_max_rolling_window()` 静态解析窗口用于扩预热窗

**`factor_engine.py`（FactorEngine）**
- **18 个内置因子**，4 类：技术面（`momentum_1d/5d/20d`、`volatility_20d`、`volume_ratio_20d`、`price_to_ma20`）、基本面（`pe/pb/ps_percentile` 252 日滚动分位、`roe_ttm`/`roa_ttm`、`revenue_growth`/`profit_growth`）、资金面（`money_flow_strength`、`big_order_ratio`、`money_flow_momentum`）、筹码面（`chip_concentration`、`winner_rate_change`）。
- **声明式数据源路由** `BUILTIN_FACTOR_SOURCES` + `DATA_SOURCE_LOADERS`；`calculate_all_factors` 内多因子共享 `data_cache`。
- **预热窗口**：`CALENDAR_DAYS_PER_TRADING_DAY=1.6`、`PREHEAT_BUFFER_DAYS=30`，起点 `start_date - 252 天`。
- **财务因子 Point-in-Time**：逐季生成快照，`trade_date` 取**公告日**（`f_ann_date` 与 `ann_date` 取孰晚，缺失退回 `end_date`），再向后对齐到第一个交易日；ROE 分母取最近 2 期净资产均值。
- **截面统计** `_calculate_factor_stats`：严格按 `(factor_id, trade_date)` 单日截面分组算 `percentile_rank`（0-100）与 `z_score`。
- **因子池过滤** `filter_universe_asof`：按 `list_date<=trade_date` 且未退市过滤（消除幸存者偏差/次新污染）。
- 落库：`FactorRepository.save_values`（`factor_values` **按 trade_date 分区**，数值列统一 `float32`）。

**`factor_analyzer.py`**：`ic_analysis`（截面 Spearman IC → ICIR/t 值/胜率，未来收益用**后复权价** `close.shift(-period)/close-1`）、`quantile_analysis`（百分位秩分组 + 多空价差年化 `×252/period`）、`correlation_matrix`（截面秩相关按日平均）。有效日门槛 `min_stocks=10`。

### 7.2 本地自算技术因子（`stk_factor_derived.py`，本项目新增）

不依赖任何外部行情源，读已落盘 `daily_history/daily` 自算：
- **预热** `PREHEAT_TRADING_DAYS=120`
- **复权**：数仓 `lake_kline.forward_factor`（前复权因子，最新=1）→ `P_qfq = P×ff`、`P_hfq = P×(ff/该股最早ff)`；数仓不可用时**退化为 1.0 不报错**
- **指标口径**（通达信/同花顺，`SMA(X,N,1)` ≙ `alpha=1/N` 的 EWM）：
  - MACD：EMA12/EMA26 → DIF → DEA=EWM(DIF,9) → MACD=2×(DIF−DEA)
  - KDJ：RSV=(C−L9)/(H9−L9)×100 → K/D=EWM(α=1/3) → J=3K−2D
  - RSI：Wilder（α=1/N，6/12/24）
  - BOLL：MA20 ± 2×STD20（`ddof=0`）
  - CCI：TP=(H+L+C)/3 → MA14 → MD=MA14(|TP−MA|) → (TP−MA)/(0.015×MD)
- 输出 35 列（与 tushare 版 `FIELDS` 逐字段一致）

### 7.3 评分 / 组合 / 回测

**`stock_scoring.py`**
- `calculate_factor_scores`：**缺数据的股票整只剔除**（不填 0，避免中性分挤掉数据完整股）；打分列优先 `z_score` 回退 `factor_value`。
- 4 种评分法：`equal_weight` / `factor_weight`（权重和为 0 抛错）/ `ml_ensemble` / `rank_ic`（按列与共识的 Spearman IC 定权）。
- `ml_based_selection` 集成：`average` / `weighted_average` / `rank_average`；**始终保留 `predicted_return` 原始量纲**供组合优化。

**`portfolio_optimizer.py`（5 种方法）**
| 方法 | 实现 |
|---|---|
| `mean_variance` | cvxpy 二次规划 `max E[r]−0.5·risk_aversion·Var`，求解器回退 `CLARABEL→SCS→OSQP`；`risk_aversion=1.0` |
| `risk_parity` | SLSQP 最小化风险贡献方差 |
| `equal_weight` | `1/n` |
| `factor_neutral` | SLSQP + 因子暴露约束 `|v·w| ≤ exposure_tolerance(1e-3)` |
| `black_litterman` | `tau=0.05` + 观点混合，再走均值方差 |

风险模型：252 交易日 → **先 ffill 再算收益**（停牌记 0 会虚假压低风险）→ 缺失填截面中位数 → **Ledoit-Wolf 收缩** → 年化 `×252`；不足则退化单位矩阵。
**截面分数→年化预期收益映射区间 `[-0.15, 0.30]`**（与回测引擎共用，防口径漂移）。

**`backtest_engine.py`（多因子组合回测）**
1. 生成调仓日（daily/weekly/monthly）
2. **两道前置校验**：交易日历为空直接拒绝；`_check_factor_coverage` 校验信号日因子覆盖率
3. 信号日选股 → 目标权重（优化失败**退回等权并 warning**）
4. **t+1 执行**：`exec_date = 下一交易日`，收盘成交
5. 可交易性：停牌不可买卖；**涨停不可买、跌停不可卖**（容差 0.2%）；涨跌停幅度：北交所 30%、创业板/科创板 20%、ST 5%、主板 10%
6. 整手 100 股；卖出回笼资金先行并预留费用；`min_trade_weight` 为调仓带宽
7. **成本**：佣金(0.001)+滑点双边，**印花税仅卖出单边**(0.0005)
8. 逐日 mark-to-market（停牌冻结最后价）；复权用 `units += Δ股数 × 真实价/复权价`
9. 指标：total_return、年化、波动率 `×√252`、夏普（**算术平均超额收益**年化）、最大回撤、胜率、Calmar、VaR95/CVaR95（历史模拟）、Beta/Alpha/信息比率（基准默认 `000300.SH`）

**`single_stock_backtest.py`**（单股择时，`/api/analysis/backtest` 用）：`ma_cross`(5/20)、`macd`、`kdj`(20/80)、`rsi`(30/70)、`bollinger`；**先在含 100 天预热的数据上算信号再截区间**；`pending_signal` 实现 t+1；`_limit_threshold=0.098`。

### 7.4 机器学习（`ml_models.py`）

- 目标类型：`return_1d/5d/20d/ranking`；模型：RandomForest / XGBoost / LightGBM。
- `prepare_training_data`：因子 pivot → 目标收益（**后复权**）→ dropna → **按时间排序**。
- `train_model`：**先切分再特征工程**（防泄漏）；`test_size=0.2`；**purge/embargo** 切除标签窗口跨界样本；预处理 `RobustScaler` + `SelectKBest(f_regression,k=20)` **只在训练集 fit**；`TimeSeriesSplit(5)` 折内重 fit。
- `predict`：**拒绝生成 ≤ `train_end_date` 的 in-sample 预测**；因子缺失即中止（不填 0）；输出 `predicted_return/probability_score/rank_score`。
- 模型落盘 `{项目根}/models/{model_id}.pkl` + `_scaler.pkl`（按 mtime 自动重载）。

**`model_training_job_service.py`**：`ThreadPoolExecutor(max_workers=2)`，进度快照 `queued→running→success/failed`，`MAX_TRACKED_JOBS=200`。

### 7.5 Text2SQL / NLP / LLM

- **`nlp_processor.py`**：6 类意图（正则 +2 分 / 关键词 +1 分打分）；实体抽取按连接词切分多条件，比较符 `greater_than/less_than/equal/between`；技术指标特殊处理（MACD 金叉/死叉、RSI 超买超卖、KDJ 金叉/死叉）；默认 `limit=20`。
- **`sql_generator.py`**：`validate_readonly_sql()` 是**统一 chokepoint**（去注释 → 拒多语句 → 首关键字必须 `SELECT/WITH` → 黑名单关键字 `INSERT/UPDATE/DELETE/DROP/ATTACH/PRAGMA/…` → 单独禁 `REPLACE INTO`）；`_build_multi_condition_sql` 动态 JOIN。
- **`text2sql_engine.py`**：**Parquet→SQLite 桥接**——把 Parquet 映射到隔离只读库 `{DATA_DIR}/text2sql_query.db`（`TABLE_COLUMNS` 定义 `stock_business/stock_factor/stock_moneyflow/stock_ma_data`），只取**最新交易日**；执行用 `sqlite3.connect('file:…?mode=ro', uri=True)`；`max_result_count=1000`。
- **`llm_service.py`**：Ollama / OpenAI(兼容) 双通道，提示词强调 SQLite 方言。

### 7.6 实时分析族

```
分钟数据同步(Baostock/通达信) → stock_minute/
  → RealtimeIndicatorEngine (10 指标, 写 realtime_events/indicators)
  → RealtimeTradingSignalEngine (读指标+行情, 8 策略, 写 realtime_events/signals)
  → RealtimeMonitorService / RealtimeRiskManager / RealtimeReportGenerator
  → WebSocketPushService → socketio 广播
```

| 服务 | 关键实现 |
|---|---|
| `realtime_data_manager.py` | 数据源解析（`tongdaxin` 默认 / `baostock`）；周期聚合 `resample`（重算 pre_close/change/pct_chg）；周期 `1/5/15/30/60min` |
| `realtime_indicator_engine.py` | 10 指标（MA/EMA/MACD/RSI/KDJ/BOLL/CCI/WR/ATR/OBV）；**分钟价调成前复权**（锚定最新日=1）；默认 `lookback_days=30`；MACD 12/26/9、KDJ 9/3/3、RSI 14、BOLL 20/2 |
| `realtime_trading_signal_engine.py` | 8 策略（ma_crossover、rsi_divergence、macd_signal、bollinger_breakout、volume_price_trend、momentum_reversal、support_resistance、trend_following）；融合净强度阈值 `±0.3`；最少 50 点 |
| `realtime_monitor_service.py` | 板块映射**优先用 `stock_basic.industry`**；异动 `anomaly_score = min(50,|chg|*5)+min(50,(vol_ratio-1)*10)`；情绪 `clamp(0,100, 50+avg_change*5+(rising_ratio-50))` |
| `realtime_risk_manager.py` | 组合风险（年化收益/波动/夏普、最大回撤、HHI、VaR/CVaR@0.95/0.99、相关性、Beta）；止损止盈 `percentage/atr/fixed`（默认 0.10/0.20）；阈值 `position_weight=0.20`、`sector_concentration=0.30`、`var_limit=0.05`、`drawdown_limit=0.15` |
| `realtime_report_generator.py` | 5 类报告 + 模板；压测 5 个默认情景 |
| `report_dispatch_service.py` | 订阅分发（不接外部网关，最小可用） |
| `websocket_push_service.py` | 7 类推送，间隔 market_data 30s / indicators·signals·risk_alerts 60s / portfolio 120s / news 300s（默认关）；**`_loop_generation` 防 stop→start 双循环** |

### 7.7 市场 / 板块 / 快照族

| 服务 | 职责与要点 |
|---|---|
| `market_snapshot_service.py` | 扶摇快照缓存聚合层；TTL：dashboard 30s / quotes 5s / indices 10s / source_status 300s；**异常回供 10 分钟旧值**；降级 `_dashboard_from_local` 标 `degraded=True` |
| `market_dashboard.py` | 纯函数：涨跌家数、6 档分布桶、榜单 TOP10；触板近似 `price/prev_close ≥ 0.995×幅度` |
| `board_market_service.py` | 涨停/跌停/炸板池（按 `(日期,页,size)` 缓存；盘中 60s、历史日永久）、连板天梯（近 30 日）、同花顺行业/概念板块（目录 6h + 快照 60s，批 100 只富化）、热榜、异动、成分股、标的检索；`_serve_stale` 过期 1 小时内回供旧值 |
| `heatmap_service.py` | 读大宽表按 `industry` 聚合**总市值加权**涨跌幅 |
| `stock_name_registry.py` | 名称补齐：本地 `stock_basic` 优先 + TickFlow `instruments` 兜底（缓存 7 天，落盘 `cache/tickflow/instruments.parquet`） |
| `wide_table_status.py` | 判定大宽表是否需更新（任一源分区日期 > 宽表日期）+ 18:00 校验（`CUTOFF_HOUR=18`） |

### 7.8 其他业务服务

| 服务 | 要点 |
|---|---|
| `stock_service.py` | 个股数据服务，全部方法带 `@cached`（stock_basic 1800s / daily 300s / financials 3600s）；**失败抛错而非返回假空**；`stk_factor` 不足时用日线**本地算** MACD/KDJ/RSI/BOLL；`screen_stocks` 基于大宽表，上限 200 |
| `pattern_screen_service.py` | 7 大类 200+ 形态字段；AND 过滤；`limit` 上限 500 |
| `trial_analytics.py` | 试用页面板：市场简报（成交额 TOP10/行业排名/主力净流入 TOP5/形态统计）、财务健康度（5 条规则 0/1 分）、个股雷达（估值/成长/技术/资金四维标准化）、个股全景、资金流统计 |
| `ai/llm_client.py` | OpenAI 兼容流式 SSE（`data:` → `delta.content`/`delta.tool_calls` 按 index 聚合） |
| `ai/tools.py` | **15 个工具**（query_data/list_data_tables/get_table_schema/list_data_jobs/run_data_job(s)/get_data_job_status/build_wide_table/get_wide_table_status/list_factors/calculate_factors/create_custom_factor/list_ml_models/train_ml_model/get_ml_training_status/predict_ml_model）；动作类只允许注册表内非 `dangerous` 任务、参数白名单 `{start_date,end_date,trade_date,full_refresh}` |
| `ai/assistant_service.py` | 对话编排：后台线程 + `queue.Queue` → SSE；空闲 15s 发心跳；`max_iterations=10`；历史 `HISTORY_MESSAGE_LIMIT=16` |
| `tongdaxin/` | `client.py`（`create_hq_api`/`connect_first_available`/`connected_session`）、`code_mapping.py`（代码风格互转）、`bars.py`（`period_type_to_tdx_category` + `normalize_tdx_minute_bars`） |

---

## 8. HTTP 接口全清单

> 两种响应契约：**信封 `{code,message,data}`**（code===200 成功，用于 stock/market/analysis/trial/pattern-screen）与**裸 `{success,...}`**（ml-factor / realtime-analysis / data-jobs / ai-assistant / text2sql / websocket）。

### 8.1 主 API `/api`（`api/stock_api.py`、`analysis_api.py`、`trial_api.py`）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/stocks` | 股票列表；`industry,area,search,page,page_size(≤100)` |
| GET | `/api/stocks/{ts_code}` | 个股基本信息 |
| GET | `/api/stocks/{ts_code}/history` | 日线历史；`start_date,end_date,limit(60)` |
| GET | `/api/stocks/{ts_code}/factors` | 技术因子 |
| GET | `/api/stocks/{ts_code}/moneyflow` | 资金流 |
| GET | `/api/stocks/{ts_code}/cyq` | 筹码分布 |
| GET | `/api/stocks/{ts_code}/financials` | 财务三表 |
| GET | `/api/stocks/{ts_code}/company` | 公司资料 |
| GET | `/api/industries`、`/api/areas` | 行业/地域列表 |
| POST | `/api/analysis/screen` | 股票筛选（基于大宽表，上限 200） |
| POST | `/api/analysis/backtest` | **单股技术策略回测**（`SingleStockBacktestEngine`）；历史不足 30 条报 400 |
| GET | `/api/trial/{market-brief,financial-health,moneyflow,stock-radar,stock-panorama,heatmap}` | 试用页数据（`trial_analytics`） |

### 8.2 多因子 `/api/ml-factor`（`api/ml_factor_api.py`）

**因子**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/factors/calculate` | 计算因子值；`trade_date,factor_ids[],ts_codes[]`（无 ts_codes 时按日期过滤股票池） |
| POST | `/factors/custom` | 创建自定义因子（先过表达式白名单校验） |
| GET | `/factors/custom-capabilities` | 表达式能力说明 |
| GET | `/factors/list` | 因子列表 |
| GET | `/factors/latest-coverage-date` | 因子集合最近覆盖日 |
| GET | `/factors/builtin-validation-samples` | 内置因子样例校验说明 |

**模型**：`POST /models/create`、`POST /models/train`（返回 202 + job_id）、`GET /models/{id}/training-date-range`、`GET /models/train-jobs/{job_id}`、`POST /models/predict`、`POST /models/evaluate`、`GET /models/list`、`GET /models/{id}`、`DELETE /models/{id}`

**评分**：`POST /scoring/factor-based`（方法 `equal_weight/factor_weight/ml_ensemble/rank_ic`）、`POST /scoring/ml-based`（`model_ids`，集成 `average/weighted_average/rank_average`）、`GET /scoring/latest-trade-date`、`GET /scoring/latest-prediction-date`

**高级分析**：`POST /analysis/factor-contribution`、`POST /analysis/sector`、`POST /factor/analysis/ic`（IC/IR）、`POST /factor/analysis/quantile`（分层回测）、`POST /factor/analysis/correlation`、`GET /analysis/{model-performance,factor-effectiveness,portfolio-performance,risk-analysis}`、`POST /analysis/generate-report`、`GET /analysis/export-report`

**批量**：`POST /batch/calculate-and-score`、`POST /batch/train-and-predict`

**组合**：`POST /portfolio/optimize`（方法 `mean_variance/risk_parity/equal_weight/factor_neutral/black_litterman`）、`GET /portfolio/list`、`GET /portfolio/{id}`、`POST /portfolio`（建仓）、`PUT/DELETE /portfolio/{id}/positions/{pid}`、`POST /portfolio/{id}/refresh-prices`（通达信实时价）、`POST /portfolio/save-optimized`、`POST /portfolio/rebalance`、`POST /portfolio/rebalance/apply`、`POST /portfolio/integrated-selection`（选股+优化一体）、`DELETE /portfolio/{id}`

**回测**：`POST /backtest/run`（`mode=sync/async`；async 返回 `run_id`+`status_url`）、`GET /backtest/runs/{id}`（孤儿 run 自愈标失败）、`GET /backtest/runs/{id}/result`、`POST /backtest/compare`

### 8.3 数据任务 `/api/data-jobs`（`api/data_jobs_api.py`）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/submit` | 提交任务；`{job_type, params}`；`wide_table_builder` 需过 18:00 校验 |
| GET | `/jobs` | 任务定义列表（`include_hidden`） |
| GET | `/list` | 运行记录；`limit,status` |
| GET | `/{run_id}` | 运行详情 |
| POST | `/{run_id}/retry` | 以原 params 重试 |
| GET | `/init-status` | 数据资产初始化状态 |
| GET | `/wide-table/status` | 大宽表状态（是否需更新 / 是否过 18:00） |
| POST | `/wide-table/build` | 构建大宽表（强校验 18:00；成功后清缓存） |

### 8.4 实时分析 `/api/realtime-analysis`

**分钟数据（`realtime_analysis.py`）**：`POST /data/sync`、`POST /data/sync-multiple`、`POST /data/sync-all-periods`、`POST /data/batch-sync`、`POST /data/aggregate`、`GET /data/{stock-list,sync-status,quality,latest,range,price,market-overview,periods,stocks,stats}`

**技术指标（`realtime_indicators.py`，前缀 `/indicators`）**：`POST /calculate`、`POST /multi-period`、`POST /batch-calculate`、`POST /compare`、`POST /cleanup`、`GET /{latest,history,supported,stats}`

**交易信号（`realtime_signals.py`，前缀 `/signals`）**：`POST /generate`、`POST /fuse`、`POST /backtest`、`POST /batch-generate`、`POST /update-status`、`POST /expire-old`、`POST /multi-stock-fusion`、`GET /{active,history,performance,strategies,stats,signal-strength-distribution}`

**实时监控（`realtime_monitor.py`，前缀 `/monitor`）**：`GET /{overview,quotes,sectors,anomalies,sentiment,alerts,heatmap,top-movers,market-stats}`、`POST /watchlist`

**实时风险（`realtime_risk.py`，前缀 `/risk`）**：`POST /portfolio-risk`、`GET /position-monitor`、`POST /stop-loss-take-profit`、`GET/POST /alerts`、`PUT /alerts/{id}/resolve`、`POST/GET/PUT/DELETE /portfolio…`、`GET/PUT /risk-thresholds`、`POST /batch-update-prices`、`POST /stress-test`

**实时报告（`realtime_report.py`，前缀 `/reports`）**：`POST /generate-report`、`GET/POST/PUT/DELETE /reports`、`/templates`、`/subscriptions`、`POST /subscriptions/dispatch`、`GET /{report-types,schedule-types,statistics}`

### 8.5 行情与数据源（`api/market_api.py`）

**`/api/market`**：`GET /snapshot`（≤200 只）、`/dashboard`、`/indices`、`/dragon-tiger`、`/auction-benchmark`、`/limit-up/ladder`、`/limit-up/pool`、`/limit-down/pool`、`/limit-break/pool`、`/hot-stocks`、`/hot-stocks/history`、`/hot-stocks/rank-trend`、`/anomaly-analysis`、`/anomaly-analysis/stocks`、`/ticker-search`、`/boards`（`tag=industry|cn_concept`）、`/boards/constituents`

**`/api/datasources`**：`GET /status`（三源健康状态，缓存 5 分钟，`force=1` 强刷）

### 8.6 其他

| 蓝图 | 路径 | 说明 |
|---|---|---|
| `text2sql` | `GET /api/text2sql/`（页面）、`POST /query`、`GET /suggestions`、`GET/POST/PUT/DELETE /templates`、`GET/POST /dictionary`、`GET /statistics`、`POST /validate`、`POST /export`、`GET /llm/status`、`POST /llm/test` | 自然语言查数（`sql_template` 强制只读 SELECT 校验） |
| `ai-assistant` | `GET /status`、`GET/POST/DELETE /sessions`、`GET /sessions/{id}/messages`、**`POST /chat`（SSE 流式）** | AI 工作台；事件 `session/token/tool_call/tool_result/heartbeat/done/error` |
| `websocket` | `GET /status`、`GET /connections`、`GET/PUT /push-config`、`POST /{start,stop,push,test-connection}`、`GET /supported-types` | 推送管理 |
| `pattern-screen` | `GET /api/pattern-screen/groups`、`POST /screen` | 形态选股 |

### 8.7 页面路由（Jinja）

- `main_bp`（无前缀）：`/`、`/stocks`、`/stock/<ts_code>`、`/analysis`、`/screen`、`/backtest`、`/ai-workbench`、`/trial/{market-brief,financial-health,stock-radar,stock-panorama,moneyflow,feature-intro}`、`/data-management`
- `/ml-factor/{,dashboard,factors,models,scoring,portfolio,analysis,backtest}`
- `/realtime-analysis/{,indicators,signals,monitor,risk,reports,websocket}`（`/data`、`/risk-management`、`/report-management`、`/websocket-management` 为重定向）
- `/heatmap/`、`/pattern-screen/`

---

## 9. WebSocket

**事件处理器**（`app/websocket/websocket_events.py`，导入即注册）

| 事件 | 行为 |
|---|---|
| `connect` / `disconnect` | 登记/清理客户端（`connected_clients`） |
| `subscribe` / `unsubscribe` | `{type, params}` → 加入/离开房间 `f"{type}_{symbol or 'all'}"`；合法 type：`market_data,indicators,signals,monitor,risk_alerts,portfolio,news` |
| `ping` / `get_status` | 心跳 / 状态查询 |

**服务端广播**：`market_data_update`、`indicators_update`、`signals_update`、`monitor_update`、`risk_alert`、`portfolio_update`、`news_update`（对应房间见源码）。

**推送服务**（`services/websocket_push_service.py`）：`start/stop_push_service()` + `socketio.start_background_task(_push_loop, generation)`；每 10s 检查各类型间隔；7 类推送（news 默认关，不造模拟新闻）。

---

## 10. 前端

### 10.1 工程

| 项 | 内容 |
|---|---|
| 技术栈 | React 19 + Vite 7 + TS 5.8 + TailwindCSS 3.4（`preflight:false`，与 Bootstrap 5 共存） |
| 图表 | **ECharts 6**（通用，按需注册）+ **lightweight-charts 5**（K线/资金曲线） |
| 数据 | `axios`（统一 `baseURL='/api'`）+ `@tanstack/react-query`（staleTime 5s） |
| 实时 | `socket.io-client`（推送管理页） + `fetch` SSE（AI 对话） |
| 路由 | `react-router-dom` 7，全部页面 `React.lazy` 代码分割 |
| 状态 | **无第三方 store**；服务端态用 react-query，本地态 `useState`/`useEffect`（`useLatestEffect` 防竞态）；主题 `ThemeContext`（localStorage `qa-theme`）；自选股/选股模板用 localStorage |
| 代理 | `vite.config.ts`：`/api` → `http://127.0.0.1:5000`；`/socket.io` → 同目标且 **`ws: true`** |
| 构建 | `npm run build`（`tsc -b && vite build`）→ `dist/`（已构建，入口 ~279KB，EChart chunk ~689KB） |

### 10.2 API 调用层（`src/api/`）

`client.ts` 提供两套封装：**信封**（`apiGet/apiPost/apiPut/apiDelete` + `apiGet/PostEnvelope`）与**裸响应**（`rawGet/rawPost/rawPut/rawDelete`）；含 `sanitizeBareNan`（清洗裸 NaN/Infinity）与 `extractApiError`。
按领域拆分：`stocks.ts`、`stockExtra.ts`、`analysis.ts`、`market.ts`、`patternScreen.ts`、`trial.ts`、`mlFactor.ts`、`realtime.ts`、`dataJobs.ts`、`ai.ts`、`types.ts`。

### 10.3 页面清单

| 路径 | 组件 | 功能 | 主要接口 |
|---|---|---|---|
| `/` | `HomePage` | 首页概览 | `/api/stocks,industries,areas` |
| `/stocks`、`/stock/:tsCode` | `StocksPage`、`StockDetailPage` | 列表 / 详情（多 Tab） | `/api/stocks*` |
| `/analysis` | `AnalysisPage` | 技术分析（K线+MACD/KDJ/RSI/BOLL） | `/api/stocks/{code}/*` |
| `/screen`、`/backtest` | `ScreenPage`、`BacktestPage` | 选股 / 单股回测 | `POST /api/analysis/{screen,backtest}` |
| `/ml-factor` | `MlFactorIndexPage` | 因子管理 | `/api/ml-factor/factors/*` |
| `/ml-factor/models` | `MlModelsPage` | 模型管理（训练 Job 轮询） | `/api/ml-factor/models/*` |
| `/ml-factor/scoring` | `MlScoringPage` | 评分选股 | `/scoring/*` |
| `/ml-factor/portfolio` | `MlPortfolioPage` | 组合持仓/优化/再平衡 | `/portfolio/*` |
| `/ml-factor/analysis`、`/backtest` | `MlAnalysisPage`、`MlBacktestPage` | 分析报告 / 组合回测 | `/analysis/*`、`/backtest/*` |
| `/realtime-analysis/{indicators,signals,monitor,risk,reports,websocket}` | `Rt*Page` | 实时六页 | `/api/realtime-analysis/*`、`/api/websocket/*` |
| `/market/{dashboard,watchlist,limit-up,hot,concepts,industries,dragon-tiger}` | `MarketDashboardPage`、`WatchlistPage`、`LimitUpLadderPage`、`HotStocksPage`、`ConceptAnalysisPage`、`IndustryAnalysisPage`、`DragonTigerPage` | 市场七页 | `/api/market/*` |
| `/data-management`、`/datasources` | `DataManagementPage`、`DataSourceCenterPage` | 数据任务编排 / 数据源状态 | `/api/data-jobs/*`、`/api/datasources/status` |
| `/heatmap`、`/pattern-screen`、`/moneyflow`、`/market-brief`、`/financial-health`、`/stock-radar`、`/stock-panorama`、`/feature-intro` | 对应 Page | 试用工具（侧栏隐藏） | `/api/trial/*`、`/api/pattern-screen/*` |
| `/ai-workbench`、`/text2sql` | `AiWorkbenchPage`、`Text2SqlPage` | AI 工作台（SSE）/ 智能查数 | `/api/ai-assistant/*`、`/api/text2sql/*` |

---

## 11. 关键口径与设计约束（必读）

1. **复权口径**：因子/ML 标签/回测一律用**后复权价**（`stk_factor.close_hfq`），覆盖率 <50% 时整只退回不复权；**实时分钟指标用前复权**（锚定最新日=1）。
2. **防前视偏差**：表达式引擎禁负 periods、禁 `rank`；财务因子按**公告日**打点；回测 **t+1 收盘成交**；组合优化传 `as_of_date` 截断协方差；ML 预测**拒绝 in-sample**；训练 purge/embargo。
3. **截面前视防护**：因子 z-score/分位严格按 `(factor_id, trade_date)` 单日截面分组。
4. **缺失数据不填 0**：打分/预测遇缺因子**整只剔除或中止**（填 0 等于给中性分，会污染排名）。
5. **成本口径**：佣金(0.001)+滑点双边，**印花税仅卖出单边**(0.0005)；换手 `0.5×Σ|Δw|`。
6. **年化口径**：统一 252 交易日；夏普分子用**算术平均超额收益**；无风险利率 `0.03`（年化）/`÷252`（日频）。
7. **涨跌停近似**：北交所 30%、创业板/科创板 20%、ST 5%、主板 10%（快照看板不含 ST；回测含 ST）。
8. **并发/缓存**：Parquet 单文件按 mtime 失效；状态表读-改-写全程 `flock`（**禁嵌套**）；text2sql 查询库物理隔离且只读；事件表重写"先写新后删旧"。
9. **去 Celery 化**：数据任务/模型训练/推送均为**进程内线程**，`DATA_JOB_EXECUTION_MODE` 仅作兼容。
10. **写 Parquet 的坑**：pyarrow **无法写"无子字段的空 struct"**（如 `params={}` 会抛 `ArrowNotImplementedError`）；`ParquetStateStore.write_frame` 已统一把 dict/list 序列化为 JSON 字符串。

---

## 12. 数据源现状与缺口

### 12.1 三源能力矩阵（实测 + 官方文档）

| 能力 | fuyao（扶摇） | tickflow | 通达信数仓 | 本地自算 |
|---|---|---|---|---|
| 日线 OHLCV | ✅ dump / `historical_kline` | ✅ `klines` | ✅ `lake_kline` | — |
| 复权因子 | ✅ adjustment-factors dump | ✅ `ex_factors` | ✅ `forward_factor` | — |
| 交易日历 | ✅ `trading_days`（近 1 年） | ❌ | ❌ | 由日线推导 |
| 股票清单/名称 | ✅ snapshot | ✅ `instruments`（含股本） | ✅ `dim_stock` | — |
| 估值（pe_ttm/pb/ps_ttm） | ✅ `valuations_snapshot` | ❌ | ❌ | 可由市值+财务推导 |
| 总市值 | ❌ | ❌ | ✅ `GP16`（万元） | — |
| 换手率/流通股本 | ❌ | ⚠️ `instruments.ext` 有 `float_shares` | ❌ | 需股本 |
| 资金流（大中小单） | ❌ 官方标注"当前不可用于外部调用" | ❌ | ❌ | 无法 |
| 筹码分布 | ❌ | ❌ | ❌ | 需换手衰减模型 |
| 财务三表 | ✅ `financial_statement` | ❌ | ❌（`fact_daily_gp_one` 已停用） | — |
| 涨停/连板/封单 | ✅ 涨停池 + 连板天梯 | ❌ | ✅ GP14/15/22/24/38/39/40 | — |
| 龙虎榜/热榜/异动/竞价 | ✅ | ❌ | ❌ | — |

### 12.2 当前落盘进度

| 资产 | 状态 | 来源 |
|---|---|---|
| `stock_basic.parquet` | ✅ 5571 只 | fuyao `stock_basic_fuyao` |
| `stock_trade_calendar.parquet` | ✅ 242 行（2025-09-12~2026-09-11） | fuyao `trading_days()` 生成 |
| `daily_history/daily` | ✅ **242 分区 / 132 万行** | fuyao `daily_history_fuyao` |
| `stk_factor/daily` | ✅ 已验证（35 列自算正确） | `stk_factor_derived`（本项目新增） |
| `daily_basic/daily` | ❌ 缺 | 需 fuyao 估值 + 数仓 GP16 拼装 |
| `moneyflow` / `cyq_perf` | ❌ 三源全无 | 只能自算或放弃 |
| `stock_business.parquet`（大宽表） | ❌ 缺 | 依赖 daily_basic/stk_factor/moneyflow |

### 12.3 无 tushare 时的建议路线（按性价比排序）

1. **`stk_factor_derived`（已完成）**：零外部依赖，直接自算 35 列技术因子 + 复权价。
2. **`daily_basic` 替代**（可做，字段有损）：`close` ← daily_history；`pe_ttm/pb/ps_ttm` ← fuyao `valuations_snapshot`（**仅最新快照**，无法回补历史）；`total_mv` ← 数仓 `GP16`；`volume_ratio/换手率` ← 自算（换手率需股本科目，缺则留空）。
3. **`trade_calendar` 替代**（已完成）：fuyao `trading_days()`，注意**只有近一年**，历史回补受限。
4. **`moneyflow` / `cyq_perf`**：三源均无 → 要么自建模型（筹码可用换手衰减估算），要么明确放弃这两项下游能力。

### 12.4 本项目已做的改动（相对上游 master）

| 文件 | 改动 | 原因 |
|---|---|---|
| `run.py` | 顶部强制 UTF-8 | GBK 控制台打印 ⚠ 会崩（本次之前已有） |
| `run_system.py` | `'scikit-learn'`→`'sklearn'`；补 UTF-8 保护 | 包名当模块名的笔误；GBK 下崩 |
| `app/services/parquet_state_store.py` | 新增 `_jsonify_object_columns()` | pyarrow 无法写空 struct（`params={}`） |
| `app/utils/stk_factor_derived.py` | **新增脚本** | 无外部源时自算技术因子 |
| `app/services/data_jobs/registry.py` | 注册 `stk_factor_derived` | 让新任务可见可提交 |
| `.env` | `SQLITE_DATABASE_PATH` 改绝对路径 | Flask-SQLAlchemy 3.x 相对路径会拼到 instance_path 下 |

---


