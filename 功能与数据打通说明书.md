# quantitative_analysis 功能与数据打通说明书

> 目标：说清**每个功能模块做什么、怎么实现的、需要哪些数据、数据怎么打通、目前缺什么**。
> 本文所有结论均来自源码与本机实际数据体检，标注了文件路径与行号，可逐条复核。
> 与 `技术文档.md` 的分工：那份讲"代码怎么写的"，这份讲"功能怎么跑通、数据从哪来"。

---

## 目录

1. [你有的数据源](#1-你有的数据源)
2. [数据层全景：一张表看懂](#2-数据层全景一张表看懂)
3. [打通路径：依赖 DAG 与初始化顺序](#3-打通路径依赖-dag-与初始化顺序)
4. [逐模块详解](#4-逐模块详解)
   - 4.1 [数据下载与任务编排](#41-数据下载与任务编排)
   - 4.2 [大宽表 stock_business](#42-大宽表-stock_business)
   - 4.3 [选股筛选](#43-选股筛选)
   - 4.4 [因子引擎](#44-因子引擎)
   - 4.5 [打分与排名](#45-打分与排名)
   - 4.6 [策略回测](#46-策略回测)
   - 4.7 [组合优化](#47-组合优化)
   - 4.8 [ML 训练与预测](#48-ml-训练与预测)
   - 4.9 [AI 问数（Text2SQL + 智能工作台）](#49-ai-问数text2sql--智能工作台)
   - 4.10 [实时行情与分钟线](#410-实时行情与分钟线)
   - 4.11 [实时监控与异动](#411-实时监控与异动)
   - 4.12 [WebSocket 推送](#412-websocket-推送)
5. [当前数据实测状态](#5-当前数据实测状态)
6. [功能缺口总表：什么跑不了、为什么](#6-功能缺口总表什么跑不了为什么)
7. [打通清单：先做什么后做什么](#7-打通清单先做什么后做什么)

---

## 1. 你有的数据源

| 源 | 凭证 | 提供什么 | 项目中的客户端 |
|---|---|---|---|
| **扶摇（同花顺官方）** | `FUYAO_API_KEY` | 全市场快照、日K dump（10天/10年）、单标的日K、财务三表、估值快照、交易日历、**涨停/跌停/炸板/连板/热榜/龙虎榜/异动原因**（实时） | `app/utils/data_sources/fuyao_client.py` |
| **TickFlow（付费）** | `TICKFLOW_API_KEY` | 日K、实时报价、除权因子、三所标的维表（含名称） | `app/utils/data_sources/tickflow_client.py` |
| **通达信本地数仓（DuckDB）** | 无（本地文件） | `dim_stock`（代码/名称/市场/上市日/涨跌停幅）、`lake_kline`（K线+`forward_factor` 前复权因子）、`lake_gpjy`（GP 系列，含 `GP16_1` 总市值） | `user/数据/tdx_data.duckdb`，只读 |
| **通达信行情服务器（pytdx）** | 无（公网 4 host:7709） | 分钟线（5/15/30/60min，**1min 不支持**） | `app/services/tongdaxin/` |
| **Baostock** | 无 | 分钟线（1min 会自动降级为 5min） | `app/services/minute_data_sync_service.py` |
| **本地自算（derived）** | 无 | 技术因子、资金流估算、筹码分布、均线 | 各 `*_derived.py` |

> ⛔ **Tushare 已彻底移除**：registry 里 18 个任务全部是 `fuyao`(11) / `derived`(7)，tushare 出现次数为 0；服务层的 tushare 提示与工具门控也已清除。

---

## 2. 数据层全景：一张表看懂

### 2.1 单文件表（`data/*.parquet`）

| 表 | 列 | 生产者 | 主要消费者 |
|---|---|---|---|
| `stock_basic.parquet` | `ts_code, symbol, name, area, industry, list_date, delist_date, list_status` | `stock_basic_fuyao.py`（扶摇快照 + 数仓 `dim_stock` 回填 name/list_date/status） | 选股（行业/地域/名称）、回测（ST判定）、监控（板块/名称）、因子（股票池）、分钟同步（代码清单） |
| `stock_trade_calendar.parquet` | `exchange, cal_date, is_open, pretrade_date` | `trade_calendar_fuyao.py`（扶摇 `trading_days`，**固定近一年≈242日**） | 所有日频任务的日期基准、财务公告日对齐、18:00 闸门 |
| `stock_business.parquet` | 47 列（见 §4.2） | `wide_table_builder.py` | 选股、AI 问数三张虚拟表 |
| `stock_company.parquet` | `ts_code, exchange, chairman, …` | **无生产者**（三源都不提供，注册项已移除） | `stock_service.py:271` |
| `stock_ma_data.parquet` | `ts_code, ma5…ma120, ema5…ema120` | `ma_calculator.py`（`dangerous=True`，只能手动跑） | 均线选股、AI 问数 `stock_ma_data` |
| `index_basic.parquet` | — | **本项目无生产者** | `get_index_basic`（无调用方） |

### 2.2 日频分区表（`data/<表>/daily/year=/month=/day=/data.parquet`）

| 表 | 生产者 | 源 | 关键列 |
|---|---|---|---|
| `daily_history/daily` | `daily_history_fuyao.py` | 扶摇 dump / 单标的K线 | `open high low close pre_close change pct_chg vol(手) amount(千元)` |
| `daily_basic/daily` | `daily_basic_fuyao.py` | 本地日线 + 数仓 GP16 + 扶摇估值 | `close volume_ratio total_mv total_share pe_ttm pb ps_ttm`（**turnover_rate/circ_mv/pe/ps/dv 恒 NaN**） |
| `stk_factor/daily` | `stk_factor_derived.py` | 本地日线自算 + 数仓 `forward_factor` | 35 列：技术指标 + `adj_factor` + `close_hfq`（后复权） |
| `moneyflow/daily` | `moneyflow_derived.py` | 本地日线 Chaikin 估算 | `net_mf_amount net_mf_vol`（**16 个分层列恒 NaN**） |
| `cyq_perf/daily` | `cyq_perf_derived.py` | 三角分布 + 换手衰减自算 | `cost_5pct/15/50/85/95 pct weight_avg winner_rate his_low his_high` |

### 2.3 财务三表（按报告期 `end_date` 分区）

| 表 | 生产者 | 源 |
|---|---|---|
| `income_statement/` `balance_sheet/` `cash_flow/` | `financial_fuyao.py` | 扶摇单标的接口，按披露截止日增量（Q1→4/30、H1→8/31、Q3→10/31、FY→次年4/30） |

### 2.4 状态/事件/缓存（非行情）

| 路径 | 用途 |
|---|---|
| `ml_factor_state/factor_definitions.parquet` | 因子定义 |
| `ml_factor_state/factor_values/trade_date=…/` | **因子值**（选股打分、回测、ML 的全部输入） |
| `ml_factor_state/model_definitions.parquet` / `ml_predictions.parquet` | ML 模型定义与预测 |
| `ml_factor_state/backtest_runs.parquet` / `backtest_results.parquet` | 回测记录与结果 |
| `ml_factor_state/portfolio_positions.parquet` | 模拟组合持仓 |
| `data_job_state/data_job_runs.parquet` | 数据任务运行记录 |
| `realtime_events/{indicators,signals}/…` | 实时指标与信号 |
| `stock_minute/{周期}/year=/month=/day=/` | 分钟线 |
| `cache/fuyao/*_k_*.parquet` | 扶摇日K dump 缓存（10d 约 1MB，全量约 172MB） |

> ⚠️ **两套"持仓"并存**：`PortfolioPosition`（SQLite ORM）与 `portfolio_positions.parquet` 同名不同库，运行时数据走 Parquet。

### 2.5 SQLite 只存应用状态

主库 `instance/stock_cursor.sqlite3`：
- **真实在用**：`table_metadata` `field_metadata` `query_template` `query_history` `business_dictionary`（Text2SQL）、`ai_chat_session` `ai_chat_message`（AI 工作台）、`risk_alerts`、`report_templates` `realtime_reports` `report_subscriptions`
- **表壳（数据在 Parquet）**：`trading_signals`、`realtime_indicators`
- **已废弃镜像**：`data_job_run`（运行时走 `data_job_state/data_job_runs.parquet`）
- 另有**隔离只读库** `data/text2sql_query.db`：只放行情快照虚拟表，用 `mode=ro` 打开

---

## 3. 打通路径：依赖 DAG 与初始化顺序

```
[扶摇] snapshot ──────────────→ stock_basic.parquet ──┐
[扶摇] trading_days ──────────→ stock_trade_calendar ─┤（标的清单 + 交易日基准）
                                                       │
[扶摇] dump(10d/10年) / 单标的K线 ──→ daily_history/daily ──┬──→ daily_basic/daily
                                                            │      (+ 数仓 GP16_1 总市值
                                                            │       + 扶摇估值快照)
                                     (+ 数仓 forward_factor)├──→ stk_factor/daily（后复权价）
                                                            ├──→ moneyflow/daily（Chaikin 估算）
                                                            └──→ cyq_perf/daily（换手衰减模型）
                                                                        │
        daily_basic + stk_factor + moneyflow + stock_basic ──→ stock_business.parquet（大宽表）
                                                                        │
[扶摇] 财务三表 ──→ income/balance/cash_flow ──┐                        │
                                               ├──→ FactorEngine ──→ factor_values ──→ 打分/回测/ML
       全部日频表 ─────────────────────────────┘                        │
                                                                        ↓
                                                        选股 / AI问数 / 回测 / ML
```

**推荐初始化顺序**（registry 的 `recommended_order`）：

```
1 trade_calendar → 2 stock_basic → 4 daily_history_by_date（或 daily_history_fuyao）
→ 6 daily_basic → 7 moneyflow → 8 stk_factor → 9 cyq_perf
→ 10 wide_table_builder → 11 factor_compute
```

**任务怎么跑起来**：
- 提交：`POST /api/data-jobs/submit` → 后台线程 → **子进程** `python <script>`（`runner.py:81`）
- 日期通过环境变量传：`DATA_JOB_START_DATE` / `END_DATE` / `TRADE_DATE` / `FULL_REFRESH`
- **增量策略**：`resolve_trade_dates_with_gap_fill`（`parquet_job_helpers.py:141`）—— 不传日期时自动补"已有分区最早日 ~ 最新日之间的空缺"，**一次最多补 60 天**，更早的要从旧往新分批跑
- 超时：`DATA_JOB_TIMEOUT` 默认 3600s
- ⛔ **没有任何定时调度器**（`DATA_UPDATE_HOUR` 是死配置）→ 全靠手动/API 触发

---

## 4. 逐模块详解

### 4.1 数据下载与任务编排

**功能**：把外部数据拉成本地 Parquet。

**实现**：
- `JobRegistry`（`app/services/data_jobs/registry.py`）定义 18 个任务，`job_type` 是唯一键
- 执行：`DataJobService.submit`（`service.py:44`）→ 后台线程 → `ScriptRunner` 起子进程
- 日频表统一用 `DailyFetchJob` 骨架重试（3 次，指数退避）+ `0.12s` 节流
- 写入原子性：`save_to_parquet` / `atomic_write_parquet`（临时文件 + `os.replace`）

**重要机制**：`job_type` 名与脚本名**故意不对应**——`stock_basic`/`trade_calendar`/`daily_basic`/`moneyflow`/`stk_factor`/`cyq_perf`/`daily_history_*` 这些"tushare 命名"的任务，实际指向 **fuyao / derived 脚本**。目的是**保持 job_type 与依赖链不变，只换底层实现**。

**数据依赖**：见 §3 DAG。

---

### 4.2 大宽表 stock_business

**功能**：把最新交易日的多张表横向拼成一张，供选股与 AI 问数。

**实现**（`app/utils/wide_table_builder.py:66`）：
```
主表 = daily_basic 最新分区全列
  ← stk_factor 最新分区（改名加 factor_ 前缀，丢 close/trade_date 与 *_hfq/*_qfq，留 adj_factor）
  ← moneyflow 最新分区（只取 net_mf_amount/net_mf_vol → moneyflow_net_*）
  ← stock_basic（ts_code, name→stock_name, symbol）
  + 派生 year/month/day
→ save_single_parquet（整文件覆盖）
```

**当前产出**：5550 行 × 47 列，`trade_date=20260911`。

**关键约束**：
- **只保留最新一天**，整文件覆盖 → 传历史 `trade_date` 查询结果为空
- AI 问数的 `stock_business` / `stock_factor` / `stock_moneyflow` **三张虚拟表共用这一个文件**
- 重建后必须失效两个缓存：`ParquetDataReader.invalidate_stock_business_cache()` + `QueryExecutor.invalidate_cache()`（`data_jobs_tasks.py:77-83` 已做）
- 构建有 **18:00 闸门**（`wide_table_status.py:27`），只有过了收盘才能提交

---

### 4.3 选股筛选

**入口**：`POST /api/analysis/screen`（`app/api/analysis_api.py:7`）→ `StockService.screen_stocks`（`stock_service.py:399`）
**页面**：Jinja `screen.html` / React `ScreenPage.tsx`

**做什么**：按条件过滤，**不做打分排序**（输出顺序 = 宽表原始行序）。

**三类条件**：
1. **离散**：`industry` / `area` / `market`（按代码后缀 .SZ/.SH）
2. **范围**（13 组，`_range_filter`）：pe、pb、ps、dv_ratio、total_mv、circ_mv、turnover_rate、volume_ratio、factor_rsi_6、factor_kdj_k、factor_macd、factor_cci、moneyflow_net_amount
3. **动态**（前端传 `field_a + operator + field_b|value`，操作符 `> >= < <= = !=`）

**⚠️ 静默失效陷阱**：`_range_filter` 开头就是 `if col not in data.columns: return data`（`stock_service.py:475`）。列不存在 → **条件被跳过，不报错**，用户以为条件生效了。已知会失效的字段：
- `moneyflow_buy_lg_amount_rate`、`moneyflow_net_d5_amount`（宽表根本没有）
- `daily_close`（宽表里叫 `close`，`daily_close` 只在 text2sql 虚拟表里）
- `ma5…ma120`（在 `stock_ma_data.parquet`，不在宽表）
- 所有恒 NaN 的列（pe/ps/dv_ratio/turnover_rate/circ_mv）→ 填了 min/max 会直接得到 **0 条结果**

**数据依赖**：`stock_business.parquet` + `stock_basic.parquet`（补 industry/area/name）

**上限**：硬截断 200 条（`has_more=True`），无分页。

---

### 4.4 因子引擎

**入口**：`POST /api/ml-factor/factors/calculate`（`ml_factor_api.py:240`）、`factor_compute` 任务
**定义存哪**：`ml_factor_state/factor_definitions.parquet`（**不是 SQLite**）；内置 17 个因子只在内存字典里（`factor_engine.py:36-62`）
**因子值存哪**：`ml_factor_state/factor_values/trade_date=YYYY-MM-DD/data.parquet`

**17 个内置因子与公式**：

| 类 | 因子 | 公式 | 数据源 |
|---|---|---|---|
| 技术 | `momentum_1d/5d/20d` | `groupby(ts_code)['close'].pct_change(period)` | 后复权价 |
| 技术 | `volatility_20d` | 日收益 `.rolling(20).std()` | 后复权价 |
| 技术 | `volume_ratio_20d` | `vol / vol.rolling(20).mean()` | 后复权价 |
| 技术 | `price_to_ma20` | `close / close.rolling(20).mean() - 1` | 后复权价 |
| 估值 | `pe/pb/ps_percentile` | `rolling(252, min_periods=20).apply(percentileofscore)/100` | `daily_basic` 的 `pe_ttm/pb/ps_ttm` |
| 基本面 | `roe_ttm` | `近4季 n_income_attr_p 之和 / 近2期 total_hldr_eqy_exc_min_int 均值` | 利润表+资产负债表 |
| 基本面 | `roa_ttm` | 同上，分母 `total_assets` | 同上 |
| 基本面 | `revenue_growth` | `(近4季revenue - 前4季revenue) / 前4季revenue` | 利润表 |
| 基本面 | `profit_growth` | 同上，用 `n_income_attr_p` | 利润表 |
| 资金 | `money_flow_strength` | `(buy_lg+buy_elg - sell_lg-sell_elg) / 总买入额` | `moneyflow` **分层列** |
| 资金 | `big_order_ratio` | 大单总额 / 全部分层总额 | `moneyflow` **分层列** |
| 资金 | `money_flow_momentum` | `net_mf_amount.rolling(5).sum()` | `moneyflow` 净额 |
| 筹码 | `chip_concentration` | `(cost_95pct - cost_5pct) / cost_50pct` | `cyq_perf` |
| 筹码 | `winner_rate_change` | `winner_rate.diff(5)` | `cyq_perf` |

**防前视三件套**（基本面因子）：
- `_point_in_time_stamp`：取 `f_ann_date`/`ann_date` 孰晚，缺则 `end_date`
- `_snap_to_trade_date`：公告日向后对齐到第一个交易日
- `_prepare_quarterly_reports`：`report_type=='1'`，同报告期多版本取最新公告日

**缺失处理**：每个因子开头都有 `if 'xxx' not in data or empty: return 空DF`；因子值 NaN/±inf 一律 `dropna`。
**⚠️ 后果**：`moneyflow` 分层列恒 NaN → `money_flow_strength`、`big_order_ratio` **永远产出空表**；`money_flow_momentum` 正常。

**自定义因子**：AST 白名单引擎（`factor_expression_engine.py`），可用列 `open/high/low/close/pre_close/change_c/pct_chg/vol/amount`，方法 `pct_change/shift/diff/rolling(mean,std,max,min,sum)`，**`rank` 被刻意禁用**（整序排名=前视），负 `periods` 直接拒绝。

**预热**：`_get_factor_data` 统一 `start_date - 252 天`（`factor_engine.py:295`）。

---

### 4.5 打分与排名

**位置**：`app/services/stock_scoring.py`

| 方法 | 公式 |
|---|---|
| `equal_weight` | `composite = 各因子 z_score 均值` |
| `factor_weight` | `w 归一化后 score = Σ z_i·w_i` |
| `ml_ensemble` | 先列 z-score，再 `score = Σ norm_i·w_i / Σ\|w_i\|` |
| `rank_ic` | `IC_i = spearman(因子排名, 共识排名)`，权重 `\|IC_i\|`，`score = Σ factor_i·sign(IC_i)·\|IC_i\|/Σ\|IC\|` |

**关键设计**：`factor_scores.dropna(how='any')`（`:75`）—— **任一因子缺失就整只剔除**，注释明确说"不能填 0，否则缺数据的股票会以中性分挤进 TopN"。

---

### 4.6 策略回测

**两套引擎**：
- **多因子组合回测**：`app/services/backtest_engine.py`，`POST /api/ml-factor/backtest/run`
- **单股技术策略回测**：`app/services/single_stock_backtest.py`，`POST /api/analysis/backtest`（模板：MA交叉/MACD/KDJ/RSI/布林）

**成交撮合（重点口径）**：
- **信号日 t → 执行日 t+1 收盘价成交**（`backtest_engine.py:148`，注释：信号日收盘后才能观察的信息只能下个交易日成交）
- 成交价用 **不复权真实价** `daily.close`（`:645`）；**估值用后复权** `stk_factor.close_hfq`（`:491`，覆盖率 <50% 的票整只退回不复权）
- **整手 100 股**（`:737`）
- **涨跌停**（`_limit_pct:597`）：创业板/科创(300/301/688/689) 20%、北交所(8/4/92) 30%、ST 5%、主板 10%，容差 0.2%；**涨停不能加仓但可减仓/清仓**，跌停不能减仓则维持
- **停牌**：无价不能开仓，卖不出的持仓保留；估值冻结在最后已知价
- 费率：佣金 0.001（双边）、滑点默认 0、印花税 0.0005（仅卖出）
- 调仓带宽 `min_trade_weight`：偏离小于阈值不动仓

**绩效指标**（`_calculate_performance_metrics:853`）：总收益、年化（`(1+total)^(1/years)-1`，`years=n_obs/252`）、波动率（`std×√252`）、夏普（`(mean(r)-rf/252)×√252/std`）、最大回撤、日频胜率、Calmar、VaR95/CVaR95、Beta/Alpha/信息比率（对基准 000300.SH，可降级 399300.SZ）。

**数据依赖与最小历史**：
| 环节 | 要求 |
|---|---|
| 交易日历 | 区间内必须有 `daily` 分区目录，且至少 1 个信号日 + 其后 ≥1 交易日 |
| 因子覆盖率 | 每个信号日都要有 `factor_values`（可 `skip_factor_coverage_check` 跳过） |
| 组合优化协方差 | 每个调仓日前 **252 交易日 ≈ 13 个月**（`portfolio_optimizer.py:432`） |
| 复权 | `stk_factor.close_hfq`（没有则退回不复权，除权日净值有假回撤） |

**⚠️ 最危险的行为**：数据只有几天时**不报错**。`executed_states` 为空 → 净值恒为初始资金 → `daily_returns` 全 0 → **所有指标算出 0 且返回 `success: True`**；只要有一次真实调仓，`years=n_obs/252` 极小会让**年化收益爆炸**（5 天赚 2% → 年化 +170%）。

---

### 4.7 组合优化

**位置**：`app/services/portfolio_optimizer.py`，`cvxpy` + `scipy` + `LedoitWolf` 收缩。

| 方法 | 实现 |
|---|---|
| `mean_variance` | `max w'μ − 0.5λ·w'Σw`，`Σw=1, w≥0`，**CVXPY** |
| `risk_parity` | 最小化风险贡献方差，**SLSQP** |
| `equal_weight` | `1/n` |
| `factor_neutral` | SLSQP + 因子暴露约束 |
| `black_litterman` | τ 缩放协方差 + 观点混合 |

**期望收益口径**：回测路径把截面 z-score 做 `rank(pct=True)` 后线性映射到 **[-0.15, +0.30]**（`backtest_engine.py:388`）。

**风险模型**：`_estimate_risk_model`（`:422`）读 `daily.close` 的 **252 交易日**（≈403 日历日）→ ffill → pct_change → **Ledoit-Wolf 收缩** → ×252 年化；`min_observations = min(60, len//2)`。

**⚠️ 已知瑕疵**：样本不足时返回的单位矩阵**未乘 252**（`:448/:471` 的 return 在年化之前），与正常路径量纲差约 4 倍；且风险模型用不复权 close，与净值用的 hfq 口径不一致。

---

### 4.8 ML 训练与预测

**入口**：`POST /api/ml-factor/models/train`（`ml_factor_api.py:449`）、`/predict`（:514）、`/batch/train-and-predict`（:1013）
**服务**：`MLModelManager`（`app/services/ml_models.py:27`）

**模型**：`random_forest` / `xgboost` / `lightgbm`（每个都有 regressor/classifier 两版，但**实际只用 regressor**，`:629` 硬编码）
**目标**：`return_1d` / `return_5d` / `return_20d` / `ranking`

**特征** = `factor_list` 里的因子 → 从 `factor_values` pivot 成宽表
**标签** = 未来 period 天的**后复权**收益（`_calculate_target_returns:354`，注释明确：不用后复权会把"回避分红股"学成伪规律）

**防泄漏三件套**：
1. **时间切分**（不做随机 split）
2. **embargo**：剔除训练尾部标签窗口伸进测试期的样本（`:448`）
3. **预处理只 fit 训练集**，CV 放进 Pipeline

**存哪**：模型 `<项目根>/models/{id}.pkl` + `{id}_scaler.pkl`；预测 `ml_factor_state/ml_predictions.parquet`

**⚠️ 三道"宁可不预测"闸门**：缺任一因子直接返回空（不填 0）；预测日 ≤ `train_end_date` 拒绝（防 in-sample 假信号）；训练任务状态在**进程内存**，重启即丢。

**最小历史**：因子计算需往前 252 自然日预热；标签需要因子日之后还有 `period+1` 个交易日的行情 → **至少约 1 年日线 + stk_factor，且"未来"那段行情也要已落盘**。

---

### 4.9 AI 问数（Text2SQL + 智能工作台）

**两条独立链路**：

**A. 经典 Text2SQL（规则优先，LLM 仅兜底）** — `POST /api/text2sql/query`
```
提问 → NLPProcessor 正则意图识别（6 类：选股/因子/技术/基本面/资金流/排名）
     → SQLGenerator 模板 or 动态构建（不依赖 LLM）
     → 失败才调 LLM 兜底
     → QueryExecutor：只读校验 → Parquet 灌进隔离 SQLite → 执行
     → ResultFormatter 格式化
```
- **无 LLM 时仍能工作**（靠正则模板），命中不了返回 400，不会静默给错结果
- 只读校验三层：① 去注释 → 拒多语句 → 首关键字必须 SELECT/WITH → 黑名单关键字 ② `mode=ro` 打开 ③ 隔离库

**B. AI 智能工作台（Agent）** — `POST /api/ai-assistant/chat`（SSE）
- 纯 LLM + function calling，模型自己写 SQL，**最多 10 轮工具调用**
- 16 个工具：只读 8 个（`query_data`、`list_data_tables`、`get_table_schema`…）+ 动作 8 个（`run_data_job`、`build_wide_table`、`calculate_factors`、`train_ml_model`…）
- **未配 LLM_API_KEY 直接 400**，无规则兜底

**虚拟表**（`text2sql_engine.py:262-300`）：

| 虚拟表 | 源文件 | 列 |
|---|---|---|
| `stock_business` | `stock_business.parquet` | `ts_code, stock_name, trade_date, daily_close(→close), factor_pct_change, vol(→factor_vol), amount, pe_ttm/pe/pb, turnover_rate, total_mv, circ_mv` |
| `stock_factor` | **同上** | `macd*→factor_macd*`、`rsi_*`、`kdj_*` |
| `stock_moneyflow` | **同上** | `net_mf_amount→moneyflow_net_amount`、`net_mf_vol` |
| `stock_ma_data` | `stock_ma_data.parquet` | `ma5…ma120`（**无 trade_date**） |

**⚠️ 两个限制**：① 虚拟表**只有最新交易日一天**（`L475-477` `df['trade_date'].max()`）→ "近20日走势"答不了 ② `_extract_table_names` 只认 `FROM/JOIN`，**CTE 里的表不会被预加载**。

---

### 4.10 实时行情与分钟线

**分钟线**：
- 源：通达信 pytdx（默认，4 个公网 host）或 Baostock
- 存：`stock_minute/{1min|5min|15min|30min|60min}/year=/month=/day=/data.parquet`
- 每次拉**最近 800 根 bar**，默认窗口最近 7 天
- ⛔ **`1min` 实际永远拿不到**：Baostock 静默降级为 5min，TDX 直接抛 `ValueError`；但代码里大量默认值写死 `'1min'`（`get_realtime_price`、`get_market_overview`、多个 API）→ 这些入口**永远返回空**
- ⛔ **没有任何定时调度**，只能 REST 手动触发（`POST /api/realtime-analysis/data/sync*`）

**真·实时行情**：`MarketSnapshotService` 走**扶摇实时快照**（`/api/a-share/prices/snapshot`），缓存 30s/5s，异常时 10 分钟内供旧值，扶摇挂了降级本地日线（`degraded=True`）。涨停/跌停/炸板/连板/热榜/龙虎榜/异动原因都在 `board_market_service.py`，全部走扶摇 special-data 端点。

---

### 4.11 实时监控与异动

**位置**：`app/services/realtime_monitor_service.py`

**数据从哪来（重要）**：**不是真实行情源**，是
```
最新价/OHLCV ← 本地 stock_minute 的最新一根 bar（5min）
昨收        ← 本地 daily_history 日线
涨跌幅      = (分钟最新close - 日线昨收) / 日线昨收 × 100
```

**功能**：实时行情、板块涨跌、异动检测（急涨/急跌/放量/突破，**无涨停检测**）、市场情绪(0-100)、监控概览

**⚠️ 板块映射严重缺口**：`_initialize_sector_mapping:27` 依赖 `stock_basic.industry`。**扶摇快照不写 industry**（`fuya_normalize.py:215-223` 只产出 ts_code/symbol/name）→ 退化到硬编码 **3 个板块共 12 只股**：
```python
'银行': ['000001.SZ','600000.SH','600036.SH','601988.SH']
'食品饮料': ['000568.SZ','600519.SH','000596.SZ','600887.SH']
'电子': ['000725.SZ','002415.SH','600584.SH','000021.SZ']
```
只打一行 warning，**前端无任何提示**。

---

### 4.12 WebSocket 推送

- 事件：7 类（`market_data` / `indicators` / `signals` / `monitor` / `risk_alerts` / `portfolio` / `news`）
- 房间：`{type}_{symbol|all}`
- ⛔ **必须显式 `subscribe` 才会推**（每个 broadcast 都有 `if room_name in room_subscriptions` 守卫，无人订阅则静默不推）
- ⛔ **推送服务默认不启动**，必须 `POST /api/websocket/start`
- 推送循环每 10s 检查，各类型间隔 30~300s；**只读已存在的 Parquet 事件，不会自动触发指标/信号计算**
- 全项目只有 `websocket_management.html` 订阅，其余页面全是 REST 轮询（30s）

---

## 5. 当前数据实测状态

体检时间：2026-09-12 23:5x，`data/` 目录实际扫描结果：

| 表 | 分区数 | 最新分区行数 | 状态 |
|---|---|---|---|
| `daily_history/daily` | **242** | 5550 | ✅ 历史完整（2025-09-12 ~ 2026-09-11） |
| `daily_basic/daily` | 25 | 5550 | ✅ 已回补近 1 月；9 列恒 NaN |
| `stk_factor/daily` | 3 | 5550 | ⚠️ 只算了 3 天 |
| `moneyflow/daily` | 25 | 5550 | ✅ 已回补；16 分层列恒 NaN |
| `cyq_perf/daily` | 1 | 5550 | ⚠️ 只有最新 1 天 |
| `stock_basic.parquet` | — | 5571 | ✅ `name` 5570 / `list_date` 5562；**`industry`/`area` 缺失** |
| `stock_business.parquet` | — | 5550×47 | ✅ 已构建 |
| `stock_trade_calendar.parquet` | — | 242 | ✅ |
| `income_statement/` 等财务三表 | — | — | ❌ **不存在** |
| `factor_values/` | — | — | ❌ **不存在** |
| `stock_ma_data.parquet` | — | — | ❌ **不存在** |
| `stock_minute/` | — | — | ❌ **不存在** |
| `index_daily/` | — | — | ❌ **不存在** |
| `stock_company.parquet` | — | — | ❌ 无生产者 |
| `realtime_events/{indicators,signals}` | — | — | 空目录 |

---

## 6. 功能缺口总表：什么跑不了、为什么

| # | 缺口 | 直接影响 | 根因 | 能否补 |
|---|---|---|---|---|
| 1 | **`stock_basic` 无 `industry`/`area`** | 板块映射只有 3 板块 12 只股；行业/地域选股得 0 条；回测行业分布空 | 扶摇快照不带行业；数仓 `dim_stock` 只有 `market` 没有 `industry` | 需另找行业源（扶摇指数成分 / 数仓其他表） |
| 2 | **财务三表缺失** | `roe_ttm`/`roa_ttm`/`revenue_growth`/`profit_growth` 全空 | 未跑 `financial_fuyao`（全市场约 3 表 × 11 分钟） | ✅ 可跑 |
| 3 | **`factor_values` 缺失** | 打分选股、回测、ML **全部无输入** | 未跑 `factor_compute` | ✅ 可跑（但需先补 2、5） |
| 4 | **`stk_factor` 只有 3 天** | 回测/ML 的**后复权价**断料，除权日假跳空 | 只跑了 1~3 天 | ✅ 可回补（242 天约 10 分钟） |
| 5 | **`cyq_perf` 只有 1 天** | `chip_concentration`/`winner_rate_change` 无历史 | 自算慢（1 天约 27 秒 × 242 天 ≈ 1.8 小时） | ✅ 可回补，耗时长 |
| 6 | **`moneyflow` 分层列恒 NaN** | `money_flow_strength`/`big_order_ratio` **永远空表** | 三源都无逐笔分单数据 | ❌ 物理不可得（除非换有资金流的数据源） |
| 7 | **恒 NaN 的估值/换手列**（turnover_rate、circ_mv、pe、ps、dv） | 选股填了对应条件 → **0 条结果** | 扶摇无静态PE/股息率；无流通股本 | 部分可用 tickflow `instruments` 的股本估算 |
| 8 | **`stock_ma_data` 缺失** | 均线类问数失败、动态条件 `ma5…ma120` 静默失效 | 未跑 `ma_calculator`（`dangerous=True` 需手动） | ✅ 可跑 |
| 9 | **`stock_minute/` 缺失** | 实时监控全空、异动/情绪无数据、WS 无行情可推 | 未同步（需连 TDX/Baostock） | ✅ 可同步（需联网） |
| 10 | **`1min` 周期不可得** | `get_realtime_price`/`get_market_overview` 等默认 1min 的入口**永远空** | Baostock 降级、TDX 不支持 | ❌ 除非改默认值为 5min |
| 11 | **`index_daily` 缺失** | 回测基准 000300.SH 无数据 → **beta/alpha/信息比率 = None** | 无生产者 | 需新增指数下载（扶摇指数接口已封装） |
| 12 | **`data/data.parquet` 无生产者** | `heatmap_service`、`pattern_screen_service`、`trial_analytics` 是**死消费者** | 历史遗留 | 属已失效路径 |
| 13 | **回测短数据不报错** | 数据不足时返回 `success:True` 的"零收益"或"年化天文数字" | 无最小区间校验 | 建议加校验（需改代码） |
| 14 | **无定时调度器** | 所有数据任务只能手动触发 | 无 APScheduler | 需外部 cron/手动 |

---

## 7. 打通清单：先做什么后做什么

按"性价比"排序，每条都给出触发方式：

**第一步：补齐能补的历史（让回测/ML 有米下锅）**
```bash
# 1) 技术因子回补到与日线同长（242 天，约 10 分钟）—— 后复权价的唯一来源
set DATA_JOB_START_DATE=20250912
set DATA_JOB_END_DATE=20260911
python app/utils/stk_factor_derived.py

# 2) 基本面 + 资金流回补（各约 1 分钟）
python app/utils/daily_basic_fuyao.py
python app/utils/moneyflow_derived.py

# 3) 筹码回补（约 1.8 小时，可先只补最近 60 天）
set DATA_JOB_START_DATE=20260601
python app/utils/cyq_perf_derived.py

# 4) 财务三表（约 33 分钟，跑一次三表都同步）
python app/utils/financial_fuyao.py

# 5) 均线衍生（快）
python app/utils/ma_calculator.py
```

**第二步：产出因子值（打通选股/回测/ML）**
```bash
set DATA_JOB_START_DATE=20260601
set DATA_JOB_END_DATE=20260911
python app/utils/factor_compute.py
```

**第三步：重建大宽表（打通选股/AI问数）**
```bash
python app/utils/wide_table_builder.py      # 注意 18:00 闸门
```

**第四步：可选（联网/客户端依赖）**
- 分钟线：`POST /api/realtime-analysis/data/sync-all-periods`（需 TDX 或 Baostock 可达）
- 指数基准：需新增下载（扶摇指数接口已在 `fuyao_client.py:197-219` 封装，但**没有落盘脚本**）
- 行业字段：需新增来源（**这是当前最大的功能性缺口**）

**验证方式**：每步跑完后用
```
POST /api/data-jobs/list          看任务状态
GET  /api/ml-factor/factors/latest-coverage-date   看因子覆盖
POST /api/analysis/screen         试选股
```
