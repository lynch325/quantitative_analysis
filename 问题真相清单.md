# 问题真相清单（待补解决方案）

> 本文档只列**问题**：现象、证据（源码位置）、影响范围、根因、能否解决。
> **【解决办法】栏留空，等待填写。**
> 与 `功能与数据打通说明书.md` 的关系：那份讲"功能怎么跑通"，这份讲"哪里跑不通、为什么"。
> 所有行号基于 2026-09-13 当前工作区代码。

## 图例

- **现象**：用户在界面/接口上实际会看到什么
- **证据**：可复核的源码位置
- **影响**：哪些功能受损、损失多大
- **可否解**：✅ 补数据/改代码即可 ｜ ⚠️ 需换思路/外部依赖 ｜ ❌ 物理不可得

---

# A 类：缺数据导致功能不可用

## A1. `stock_basic` 缺 `industry` / `area` —— 板块功能形同虚设 【最高优先级】

- **现象**：板块涨跌只有 3 个板块共 12 只股；行业/地域选股返回 0 条；回测的"行业分布"为空
- **证据**：
  - `app/services/realtime_monitor_service.py:27-53` `_initialize_sector_mapping()`，缺 `industry` 时返回硬编码兜底（`:34-38`）
    ```python
    fallback = {
        '银行':    ['000001.SZ','600000.SH','600036.SH','601988.SH'],
        '食品饮料': ['000568.SZ','600519.SH','000596.SZ','600887.SH'],
        '电子':    ['000725.SZ','002415.SH','600584.SH','000021.SZ'],
    }
    ```
  - 只打一行 warning：`"stock_basic 缺少行业字段，板块映射退化为默认列表"`，**前端无任何提示**
  - `app/utils/data_sources/fuyao_normalize.py:215-223` `snapshot_rows_to_stock_basic` 只产出 `ts_code / symbol / name`
- **影响**：
  - 实时监控板块绩效（实际输出 0 个板块，因为这 12 只大概率也不在分钟线里）
  - `/monitor/heatmap` 热力图恒空
  - 选股 `industry` / `area` 条件恒 0 条
  - 回测 `industry_distribution` 空
- **根因**：扶摇全市场快照不带行业；本地数仓 `dim_stock` 只有 `market`（上海/深圳）没有 `industry`
- **可否解**：✅ **已解决**（用户方案：本地通达信数仓有完整板块分类）
- **【解决办法】** ✅ **已落地（2026-09-13）**
  - **数据源**：本地通达信数仓 `user/数据/tdx_data.duckdb` 内有完整板块体系：
    - `dim_sector`（`sector_code` / `sector_name` / `sector_type` / `is_user`）
      实测分布：**行业 110、概念 269、自定义 159、风格 152、地区 32**
    - `bridge_stock_sector`（`stock_code` + `sector_code` 联合主键 + `sector_type`），共 112581 行 / 5585 只股票
    - 另有 `fact_daily_bk` / `lake_bk`（板块日线行情，尚未使用）
  - **实现**：扩展 `app/utils/stock_basic_fuyao.py`
    - `_load_sector_lookup(con, sector_type, strip_suffix, multi)` —— `bridge_stock_sector JOIN dim_sector` 取映射
    - `_apply_lookup(frame, out_col, lookup)` —— 只补空值、不覆盖已有值
    - `_enrich_from_duckdb()` 由只回填 name/list_date/status，扩展为**同时回填 industry / area / concepts**
    - 地区值去掉"板块"后缀（"贵州板块"→"贵州"），与原有 tushare 口径一致
    - 概念为多归属，用 `;` 连接（如 `600519.SH → 通达信88;白酒概念;乡村振兴`）
  - **覆盖率**：`industry` 5566/5571（**99.9%**）、`area` 5566、`concepts` 5435
  - **效果**：`RealtimeMonitorService._initialize_sector_mapping` 从 **3 个板块 12 只股 → 110 个板块 5566 只股**
  - **未覆盖的 5 只**：`001246.SZ`、`301716.SZ`、`920025.BJ`、`920201.BJ`、`920229.BJ`（北交所/次新股）
  - **顺带产出**：可利用 `dim_sector` + `bridge_stock_sector` 做**概念板块监控**（269 个概念），以及 `fact_daily_bk` 做**板块日线行情**（A10 的基准问题也可能一并受益）
- **验证方法**：
  ```python
  df = pd.read_parquet('data/stock_basic.parquet')
  print('industry 非空:', df['industry'].notna().sum(), '/', len(df))   # 期望 5566/5571
  print('行业数:', df['industry'].nunique())                              # 期望 110
  # 抽查：600519.SH→白酒/贵州、000001.SZ→银行/深圳、300750.SZ→电气设备/福建
  ```

---

## A2. 财务三表（`income_statement` / `balance_sheet` / `cash_flow`）完全缺失

- **现象**：4 个基本面因子恒空，相关选股/回测无基本面维度
- **证据**：`data/` 下三个目录均不存在；`financial_fuyao.py:194 main()` 可产出
- **影响**：`roe_ttm` `roa_ttm` `revenue_growth` `profit_growth` 全空；`factor_compute` 里这 4 个因子产出空表
- **根因**：未执行过下载
- **可否解**：✅ 跑 `financial_fuyao.py`（扶摇已有财务三表接口）
- **【解决办法】** ✅ **已执行（2026-09-13，后台运行中）** —— 用扶摇财务接口
  - **接口**：`GET /api/a-share/financials/{income-statements|balance-sheets|cash-flow-statements}`
    参数 `thscode / period=quarterly / limit(≤20 期)`；客户端 `fuyao_client.py:255 financial_statement()`
  - **归一化**：`fuyao_normalize.financial_items_to_frame()` + `INCOME/BALANCE/CASHFLOW_FIELD_MAP`
  - **已核对的关键映射**（因子引擎依赖的列全部到位）：
    | 因子需要 | 扶摇原始字段 |
    |---|---|
    | income `revenue` | `operating_income` |
    | income `n_income_attr_p` | `parent_holder_net_profit` |
    | balance `total_assets` | `assets_total` |
    | balance `total_hldr_eqy_exc_min_int` | `holder_equity_total` |
    | `ann_date` / `end_date` | `report_date_ms` / `period_end_ms`（按北京时区解析） |
  - **口径核对（重要）**：茅台 `20260630` 营收 907 亿 = **中报累计值**（非单季），
    与 `report_type=1`（合并报表累计）一致 → TTM 计算正确
  - **`report_type` 缺失不影响**：`_prepare_quarterly_reports`（`factor_engine.py:517-521`）
    是 `if "report_type" in out.columns` **有该列才过滤**，扶摇不返回该列 → 全部保留，不误滤
  - **`f_ann_date` 缺失不影响**：`_point_in_time_stamp`（`:453`）按 `f_ann_date → ann_date → end_date` 依次回退
  - **增量策略**：按披露截止日算 expected（今天 2026-09-13 → `20260630`），每 500 标的 flush 落盘
  - **耗时**：5571 只 × 3 表 ≈ 1.7 万次请求 × 0.12s 节流 ≈ **33 分钟**（后台跑）
- **验证**：
  ```python
  # 目录出现分区
  ls data/income_statement/year=*/month=*/day=*/data.parquet
  # 抽查茅台
  df = pd.read_parquet('data/income_statement/year=2026/month=06/day=30/data.parquet')
  df[df.ts_code=='600519.SH'][['end_date','ann_date','revenue','n_income_attr_p']]
  ```
- **【解决办法】**：（已填，见上）

---

## A3. `factor_values` 完全缺失 —— 选股打分/回测/ML 全部无输入

- **现象**：因子打分选股 404「未找到因子数据」；回测报「因子值缺失：N/M 个调仓信号日」；ML 训练报「未找到因子数据」
- **证据**：`data/ml_factor_state/` 下只有 `factor_definitions.parquet`，无 `factor_values/`；
  - `app/api/ml_factor_api.py:670-671` 打分 404
  - `app/services/backtest_engine.py:304-344` `_check_factor_coverage`
  - `app/services/ml_models.py:305` `raise ValueError("未找到因子数据")`
- **影响**：**这是全系统的咽喉**——因子值缺了，选股/回测/ML 三条主链路全断
- **根因**：未执行 `factor_compute`
- **可否解**：✅ 可跑，但**依赖 A2（财务三表）与 A4（stk_factor 历史）先就绪**，否则因子值大面积为空
- **验证**：`GET /api/ml-factor/factors/latest-coverage-date`
- **【解决办法】**：

---

## A4. `stk_factor` 只有 3 天历史 —— 后复权价断料

- **现象**：回测净值在除权除息日出现"假回撤"；ML 标签被除权缺口污染；动量类因子含假跳空
- **证据**：`data/stk_factor/daily/` 只有 3 个分区，而 `daily_history/daily` 有 242 个
  - `app/services/data_reader.py:127-188` `get_return_prices`：优先 `stk_factor.close_hfq`，**单只覆盖率 <50% 整只退回不复权**
  - `app/services/backtest_engine.py:491-512` 复权调整，覆盖率 <50% 的票整只按不复权估值
- **影响**：后复权口径是**因子、ML 标签、回测估值的统一口径**（代码多处注释强调），断了会导致全链路失真
- **根因**：只跑了 1~3 天，未回补
- **可否解**：✅ `stk_factor_derived.py` 可回补（242 天约 10 分钟，复权因子取数仓 `lake_kline.forward_factor`）
- **验证**：`ls data/stk_factor/daily/year=* -Recurse | Measure` 分区数应接近 242
- **【解决办法】**：

---

## A5. `cyq_perf`（筹码）只有 1 天历史，且是**模型估算**

- **现象**：`chip_concentration` / `winner_rate_change` 无历史值
- **证据**：`data/cyq_perf/daily/` 只有 1 个分区；`app/utils/cyq_perf_derived.py` 用三角分布+换手衰减自算
- **影响**：两个筹码因子无历史；且**数值本身是模型估算**，非交易所真实筹码
- **根因**：① 三源都不提供真实筹码分布 ② 自算慢（1 天约 27 秒 → 242 天约 1.8 小时）
- **可否解**：✅ 可回补但耗时长；⚠️ 精度受限于模型假设（换手率用总股本近似，无流通股本）
- **【解决办法】**：

---

## A6. `moneyflow` 的 16 个分层买卖列恒为 NaN —— 2 个因子物理不可得

- **现象**：`money_flow_strength`、`big_order_ratio` **永远产出空表**
- **证据**：`app/utils/moneyflow_derived.py:114-115` 明确留空
  ```python
  # 分层买卖列：无数据源可得，保持 NaN
  for column in OUTPUT_COLUMNS:
      if column not in frame.columns:
          frame[column] = np.nan
  ```
  - `app/services/factor_engine.py:670-697`：这两个因子依赖 `buy_lg_amount / buy_elg_amount / sell_*`
- **影响**：2/17 个内置因子永久失效；选股的资金流条件只剩 `net_mf_amount`
- **根因**：大中小单分层需要**逐笔分单数据**，扶摇/TickFlow/数仓均无
- **可否解**：❌ **物理不可得**，除非更换/新增提供资金流分层的数据源
- **【解决办法】**：

---

## A7. 一行恒 NaN 的列 → 选股填了条件直接得 0 条

- **现象**：填了换手率/流通市值/静态PE/市销率/股息率范围 → 结果 0 条（NaN 比较恒 False）
- **证据**：`app/utils/daily_basic_fuyao.py:220-242`
  ```python
  for column in ("turnover_rate", "turnover_rate_f", "pe", "ps",
                 "dv_ratio", "dv_ttm", "float_share", "free_share", "circ_mv"):
      frame[column] = np.nan
  ```
- **影响**：13 组范围条件里，这 7 组**填了就是 0 条**（不报错，用户以为是"没有符合条件的股票"）
- **根因**：扶摇无静态PE/股息率；无流通股本（换手率算不出）
- **可否解**：⚠️ 部分可解——TickFlow `instruments` 有股本信息（可算流通市值/换手率）；静态PE/股息率需另找源
- **【解决办法】**：

---

## A8. `stock_ma_data.parquet` 缺失 —— 均线类问数与条件失效

- **现象**：AI 问"均线多头排列的股票"必失败；选股动态条件里的 `ma5…ma120` 静默失效
- **证据**：`data/stock_ma_data.parquet` 不存在；由 `ma_calculator.py:24` 产出，但 registry 标 `dangerous=True`（`registry.py:151-161`），**不在可见任务列表**，只能手动执行
- **影响**：均线问数失败；动态条件静默跳过（不报错）
- **可否解**：✅ 跑 `ma_calculator.py` 即可（快）
- **【解决办法】**：

---

## A9. `stock_minute/` 完全缺失 —— 实时监控全线为空

- **现象**：实时行情/板块/异动/情绪全部返回空数组（`success: True` 但无数据）；市场情绪返回 `success: False`
- **证据**：
  - `app/services/realtime_monitor_service.py:63` 读 `stock_minute` 最新 bar
  - `:344-348` 情绪不足时 `success: False, '没有足够的数据计算市场情绪'`
  - `:453` `_get_active_stocks()` 退化到硬编码 5 只
- **影响**：整个"实时分析"模块（监控、异动、情绪、WS 行情推送）无数据
- **根因**：未同步；且**没有任何定时调度器**，只能 REST 手动触发
- **可否解**：✅ 可同步
- **【解决办法】** ✅ **已解决（2026-09-13）** —— 改用 **TickFlow 日内分时**作为分钟线源
  - **为什么换源**：现有两个分钟源都有硬伤 —— 通达信 pytdx **不支持 1min**（`tongdaxin/bars.py:16-19` 直接抛
    `ValueError`）、Baostock 会把 1min **静默降级成 5min**（`minute_data_sync_service.py:104-107`）。
    而 TickFlow 的 `GET /v1/klines/intraday`（**Beta，pro+ 档**）可直接给出最新交易日的 1/5/15/30/60 分钟线
    （本机 key 实测为 `paid` 档）。
  - **改动 1**：`app/utils/data_sources/tickflow_client.py` 新增 `intraday()` 方法
    （项目原先只封装了 klines / quotes / ex_factors / instruments，**无法查日内分时**）。
  - **改动 2**：新增 `app/utils/minute_sync_tickflow.py`：逐只拉取 → 列式转行式 →
    `MinuteParquetStore.write_frame()` → 落 `data/stock_minute/{period_type}/year=/month=/day=/data.parquet`；
    并从本地日线（`daily_history` 最新分区）补 `pre_close / change / pct_chg`。
    参数：`DATA_JOB_PARAM_PERIOD`（1m/5m/15m/30m/60m，默认 1m）、`DATA_JOB_PARAM_SYMBOLS`（标的白名单）、
    `TF_THROTTLE_SECONDS`（节流，默认 0.15s）。
  - **改动 3**：registry 新增 `minute_sync_tickflow`（source=tickflow，group="分钟线"，**可见**）。
    注意 `1m` 与监控默认的 `DEFAULT_PERIOD_TYPE="5min"` 是**不同分区**，两者都要跑。
  - **实测（5 只标的）**：1min **1205 行**（5×241，09:30~15:00）、5min **240 行**（5×48），
    `pre_close/change/pct_chg` **全部非空**（茅台昨收 1285.13 ✓）；`RealtimeMonitorService._minute_frame`
    能取到数据、`_latest_rows` 5 只；板块映射 110 个（A1 成果）。
  - ⚠️ **踩坑（已修）**：① `client.intraday()` 的**原始返回只有 7 个键**
    （`timestamp/open/high/low/close/volume/amount`）—— `symbol/name/trade_date/trade_time` 是
    **tickflow SDK 自己派生**的列，**REST 层不返回**，所以交易日必须从已解析的 `datetime` 推导，
    曾因此导致 `pre_close` 全空；② 该返回是**列式结构**（每键一个等长数组），不能当标量 `str()` 用。
  - ⚠️ **约束**：逐只请求，全市场约 5500 次调用（脚本内置节流 + 限频重试），建议按需选择标的范围。
- **【解决办法】**：（已填，见上）

---

## A10. `index_daily/` 缺失 —— 回测基准指标恒为 None

- **现象**：回测结果里 `beta` / `alpha` / `information_ratio` 恒为 `None`
- **证据**：`app/services/backtest_engine.py:1030-1047` `_get_benchmark_returns`，依次尝试 `000300.SH → 399300.SZ`，都无则三个指标为 None（有 warning）
- **影响**：回测无法与基准对比，超额收益评估缺失
- **根因**：无指数下载脚本（`data_reader.py:571-615` 有 `_read_index_daily` 读取逻辑，**但没有生产者**）
- **可否解**：⚠️ 需新增指数下载（扶摇指数接口 `fuyao_client.py:197-219` 已封装，但无落盘脚本）
- **【解决办法】**：

---

## A11. `stock_company.parquet` 无生产者

- **现象**：`/api/stocks/{code}/company` 返回空
- **证据**：`app/utils/stock_company.py` 是 tushare 版，注册项已移除；三源均不提供（董事长/总经理/注册资本/省份）
- **影响**：公司资料展示为空（非核心链路）
- **可否解**：❌ 三源均无，或需另找工商数据源
- **【解决办法】**：

---

# B 类：代码逻辑硬伤（不是缺数据，是会给出错误结果）

## B1. 回测数据区间不足时**不报错**，返回看似正常的假结果

- **现象**：数据显示"0% 收益、0% 回撤"的完整净值曲线；或年化收益天文数字
- **证据**：
  - `app/services/backtest_engine.py:866` `if not portfolio_values or not daily_returns: return {}`
  - `executed_states` 为空 → 净值恒 = 初始资金 → `daily_returns` 全 0 → **所有指标算出 0**
  - `:1295` 仍返回 `success: True`
  - 年化爆炸：`years = n_obs/252`，5 个交易日赚 2% → `(1.02)^50.4 - 1 ≈ +170%`
  - 唯一提示是 `failed_signal_dates`（`:254`），前端不突出显示
- **影响**：**最危险的失真**——用户以为回测成功，实际是垃圾结果
- **可否解**：✅ 应加最小区间校验（如区间 < 60 交易日直接拒绝，或至少在响应里标 `degraded=True`）
- **【解决办法】**：

---

## B2. 选股：列不存在时条件**静默跳过**，用户以为条件生效了

- **现象**：填了条件但返回的是全集（或错误的集合），不报错
- **证据**：`app/services/stock_service.py:475-476`
  ```python
  def _range_filter(data, col, lo, hi):
      if col not in data.columns:
          return data          # ← 静默跳过
  ```
  - 动态条件同样：`:514` `if field_a not in df.columns: continue`
- **已知会静默失效的字段**：
  - `moneyflow_buy_lg_amount_rate`、`moneyflow_net_d5_amount`（宽表根本没有这些列）
  - `daily_close`（宽表里叫 `close`；`daily_close` 只在 text2sql 虚拟表映射里存在）
  - `ma5…ma120`（在 `stock_ma_data.parquet`，不在宽表）
  - 所有恒 NaN 的列（见 A7）
- **影响**：筛选结果不可信，且**完全无提示**
- **可否解**：✅ 应改为：列不存在时返回错误或在响应里列出"被忽略的条件"
- **【解决办法】**：

---

## B3. `1min` 周期物理不可得，但代码大量默认值写死 `'1min'`

- **现象**：多个"实时"接口**永远返回空**
- **证据**：
  - `app/services/minute_data_sync_service.py:104-107`：Baostock 遇到 1min **静默降级为 5min**
  - `app/services/tongdaxin/bars.py:16-19`：TDX 遇到 1min **直接 `raise ValueError`**
  - 但默认值写死 1min 的位置：
    - `realtime_data_manager.py:399` `get_realtime_price()`
    - `realtime_data_manager.py:410/454` `get_market_overview()`
    - `realtime_trading_signal_engine.py:43` `generate_signals(period_type='1min')`
    - `realtime_analysis.py:166/231/261/293` 多个 API
- **影响**：这些入口**永远返回"未找到实时数据"**
- **可否解**：✅ 把默认值改成 `'5min'` 即可（唯一正确设计是 `realtime_monitor_service.py:19` 的 `DEFAULT_PERIOD_TYPE = "5min"`）
- **【解决办法】**：

---

## B4. 组合优化：样本不足时返回**未年化**的单位矩阵，量纲差约 4 倍

- **现象**：数据不足时均值方差/风险平价退化成近似等权，且风险项被高估
- **证据**：`app/services/portfolio_optimizer.py`
  - `:448` 与 `:471` 的退化分支 `return` **在年化之前**（`:482-483` 的 `if annualize: cov *= 252` 没执行到）
  - 正常路径年化标准差 ≈ 0.25，退化路径 = 1.0 → 差约 4 倍
- **影响**：短数据或股票数 < 2 时，优化结果不可信
- **可否解**：✅ 退化分支也应年化，或直接返回错误
- **【解决办法】**：

---

## B5. 风险模型用**不复权 close**，与回测净值用的 `close_hfq` 口径不一致

- **证据**：`app/services/portfolio_optimizer.py:440-444` 读 `get_daily(...)` 的 `close`；而回测估值用 `close_hfq`（`backtest_engine.py:491`）
- **影响**：协方差矩阵被除权跳空污染
- **可否解**：✅ 改用 `get_return_prices`（后复权）
- **【解决办法】**：

---

## B6. `max_weight` 约束被重复施加

- **证据**：`portfolio_optimizer.py:155`（QP 内约束）与 `:520`（`_apply_constraints` 后处理）都施加 `max_weight`
- **影响**：实际权重可能比用户设置的更保守，且难以预期
- **可否解**：✅ 去重或明确文档化
- **【解决办法】**：

---

## B7. `UNSUPPORTED_CONSTRAINT_KEYS = set()` —— 未知约束键被静默忽略

- **证据**：`portfolio_optimizer.py:16`
- **影响**：用户传了拼错的约束名（如 `max_wieght`），系统不报错也不生效
- **可否解**：✅ 填入已知约束名集合，未知键报错
- **【解决办法】**：

---

## B8. 单股回测涨跌停阈值固定 `0.098`，不区分板块/ST

- **证据**：`app/services/single_stock_backtest.py:204-211`
- **对比**：多因子回测是对的（`backtest_engine.py:597-610` 区分创业板20%/北交所30%/ST5%/主板10%）
- **影响**：创业板/科创板/ST 股的涨跌停判定错误 → 可交易性判断失真
- **可否解**：✅ 复用 `_limit_pct` 逻辑
- **【解决办法】**：

---

## B9. 单股回测完全**不做复权**

- **证据**：`app/services/stock_service.py:114-134` `get_daily_history` 直接返回原始 `close`；`single_stock_backtest.py` 用它算均线/动量/收益
- **影响**：除权除息日出现假跳空，信号与收益失真，**无告警**
- **可否解**：✅ 改用 `get_return_prices`
- **【解决办法】**：

---

## B10. 存在**两套同名持仓**：SQLite ORM 与 Parquet

- **证据**：
  - `app/models/portfolio_position.py:12` `PortfolioPosition`（SQLite 表 `portfolio_positions`）
  - `app/services/parquet_state_store.py:583-584` `PortfolioRepository`（`ml_factor_state/portfolio_positions.parquet`）
  - 两者同名不同库；ORM 类未 import 到 `app/models/__init__.py`，仅 `realtime_report_generator.py:928` 引用
- **影响**：极易混淆"到底读的是哪份持仓"
- **可否解**：✅ 明确废弃其一
- **【解决办法】**：

---

## B11. WebSocket：无人订阅则**一条都不推**，且推送服务默认不启动

- **证据**：
  - `app/websocket/websocket_events.py:157-163` 每个 broadcast 都有 `if room_name in room_subscriptions` 守卫
  - `app/services/websocket_push_service.py:40` `is_running` 初始 False；`app/__init__.py:82` 只注册事件，未启动推送
  - 全项目只有 `websocket_management.html` 会 subscribe，其余页面全是 30s REST 轮询
- **影响**：WebSocket 功能"存在但默认不工作"
- **可否解**：✅ 设计如此（按需订阅），但应在文档/界面明示；启动需 `POST /api/websocket/start`
- **【解决办法】**：

---

## B12. Text2SQL 虚拟表**只有最新一天**，历史类问题答不了

- **证据**：`app/services/text2sql_engine.py:475-477` `df['trade_date'].max()`
- **影响**："近 20 日走势""上个月涨幅"等问题无法回答（AI 提示词里已写明此限制，`ai/prompts.py:69`）
- **可否解**：⚠️ 需改为按日期分区加载，改动较大
- **【解决办法】**：

---

## B13. Text2SQL 的表名抽取只认 `FROM/JOIN`，**CTE 里的表不会被预加载**

- **证据**：`app/services/text2sql_engine.py:451` `_extract_table_names` 正则 `(?:FROM|JOIN)\s+(\w+)`
- **影响**：`WITH t AS (SELECT ... FROM stock_business)` 这类 SQL 会因表未加载而报 "no such table"
- **可否解**：✅ 正则补充 CTE 识别
- **【解决办法】**：

---

## B14. 无任何定时调度器

- **证据**：`config.py:62-63` `DATA_UPDATE_HOUR/DATA_UPDATE_MINUTE` 在 `app/` 内 **0 处引用**；全仓无 APScheduler/schedule
- **影响**：所有数据任务只能手动/API 触发，无法自动日更
- **可否解**：⚠️ 需引入调度器或依赖外部 cron
- **【解决办法】**：

---

## B15. `data/data.parquet` 无生产者 —— 三个服务是死消费者

- **证据**：`heatmap_service.py:24`、`pattern_screen_service.py:211`、`trial_analytics.py:20` 读 `data.parquet`，但**该文件不存在且无任何脚本产出**
- **影响**：这三个服务一旦被调用就报错（用户实际反馈：板块热力图页显示"数据加载失败，请确认 data/data.parquet 是否存在"）
- **可否解**：✅ 补生产者
- **【解决办法】** ✅ **已解决（2026-09-13）** —— 新增生产者 `app/utils/heatmap_data_builder.py`
  - **schema 权威定义**来自 `tests/services/test_heatmap_service.py` 的 `sample_df` fixture，共 9 列：
    `ts_code / name / industry / pct_chg / close / total_mv / net_mf_amount / turnover_rate / trade_date`
    （fixture 里的行业值 `'银行'`/`'全国地产'` 与 A1 回填的通达信行业口径一致）
  - **拼装来源**（全部本地已有）：`daily_history`(close,pct_chg) + `daily_basic`(total_mv,turnover_rate)
    + `moneyflow`(net_mf_amount) + `stock_basic`(name, industry)
  - 只产出**最新一个交易日**（HeatmapService 取 `df['trade_date'].iloc[0]`）；无行业的股票剔除，避免聚合成 None 板块
  - **结果**：5550 行 / **110 个行业**，`HeatmapService` 正常返回，热力图页面恢复
  - ⚠️ **仍不可用**：`pattern_screen_service` / `trial_analytics` 还需要 `pattern_*`、`limit_status`、`consec_up_days`
    （K线形态识别、涨停状态、连板天数），本脚本**未产出这些列** → 见下方 B20 与"待定"项
- **【解决办法】**：（已填，见上）

---

## B16. LLM 配置耦合：`LLM_BASE_URL` / `LLM_MODEL` 被 ollama 与 openai 两个 provider 共用

- **证据**：`config.py:76-93`，两个 provider 都从同一组环境变量读 `base_url` 和 `model`
- **影响**：把 `LLM_BASE_URL` 设成 DeepSeek 时，ollama 的 `base_url` 也变成 DeepSeek
- **可否解**：✅ 拆成 `OLLAMA_BASE_URL` / `OPENAI_BASE_URL` 等
- **【解决办法】**：

---

## B17. AI 智能工作台未配 LLM 时**直接 400，无规则兜底**

- **证据**：`app/api/ai_assistant_api.py:88-97` 返回 400「大模型接口未配置…」
- **对比**：经典 Text2SQL 无 LLM 时仍能靠正则模板工作（`sql_generator.py:429-514` 默认模板）
- **影响**：没配 key 时整个 AI 工作台不可用
- **可否解**：⚠️ 可为工作台也加规则兜底
- **【解决办法】**：

---

## B18. ML 模型：classifier 定义了但**实际只用 regressor**

- **证据**：`app/services/ml_models.py:622-635` `_create_model`，`:629` 硬编码取 `['regressor']`
- **影响**：分类目标（`ranking` 等）实际按回归训练
- **可否解**：✅ 按 `target_type` 选择
- **【解决办法】**：

---

## B19. ML 训练任务状态存**进程内存**，重启即丢失

- **证据**：`app/services/model_training_job_service.py:20` `job_store`（上限 200 条，按 `created_at` 淘汰）；`ai/tools.py:363` 的错误文案里已写明"重启后丢失"
- **影响**：服务重启后查不到历史训练任务
- **可否解**：✅ 落 Parquet（同 `data_job_state` 的做法）
- **【解决办法】**：

---

## B20. 热力图：板块涨跌幅出现 `nan`（NaN 污染整块）

- **现象**：`data.parquet` 补上后热力图能显示，但**部分板块的涨跌幅是 `nan`**（实测"半导体"197 只成分却显示 `nan%`）
- **证据**：`app/services/heatmap_service.py:36`
  ```python
  avg_pct_chg = np.average(group['pct_chg'], weights=group['total_mv'])
  ```
  `np.average` **不会跳过 NaN** —— 只要板块内有任意一只股票的 `pct_chg` 或 `total_mv` 为 NaN，
  **整个板块的加权平均就变成 NaN**（而 `up_count`/`down_count`/`stock_count` 仍是正常数字，
  所以表现为"有成分股却没有涨跌幅"）
- **根因**：`daily_history` 的 `pct_chg` 有 1 个缺失（首日无前收），`daily_basic` 的 `total_mv` 有 2 个缺失，
  且 `turnover_rate` 恒 NaN；这些 NaN 会污染其所在板块
- **影响**：约 1~2 个板块显示不出涨跌幅（其余 108 个正常）
- **可否解**：✅ 两种修法（**待用户定**）
  1. 改 `heatmap_service`：聚合前 `group = group.dropna(subset=['pct_chg','total_mv'])`（推荐，职责清晰）
  2. 改 `heatmap_data_builder`：生成时过滤掉 `pct_chg`/`total_mv` 为 NaN 的行（不用动服务代码，但个股明细会少几只）
- **【解决办法】**：

---

# C 类：设计约束（不是 bug，但必须知道）

## C1. 交易日历只有**近一年**（扶摇固定窗口）

- **证据**：`app/utils/trade_calendar_fuyao.py` 用扶摇 `trading_days()`，实测 242 个交易日（2025-09-12 ~ 2026-09-11）
- **影响**：把财务因子公告日对齐到更早的交易日时，超出范围会失败（`_snap_to_trade_date` 会退回原值）
- **可否解**：⚠️ 扶摇接口限制；或本地生成日历（排除周末+节假日表）
- **【解决办法】**：

---

## C2. 数仓 GP16（总市值）比日线**滞后数日**

- **证据**：实测日线到 2026-09-11 时，`lake_gpjy.GP16_1` 只到 2026-09-09
- **现状**：已用 `merge_asof(direction="backward")` 取"截至该日最近一次"对齐
- **影响**：最新 1~2 天的 `total_mv` 是前值近似
- **【解决办法】**：

---

## C3. `daily_basic` 的估值只有**最新交易日**有值

- **证据**：`app/utils/daily_basic_fuyao.py:220-235`，扶摇 `valuations_snapshot` 是实时快照
- **影响**：历史日期的 `pe_ttm/pb/ps_ttm` 为 NaN → `pe/pb/ps_percentile` 因子历史上大部分为空
- **【解决办法】**：

---

## C4. 财务三表的三个 job 指向同一脚本，一次跑会同步三张表

- **证据**：`registry.py:118-126`，`income_statement`/`balance_sheet`/`cash_flow` 都指向 `financial_fuyao.py`，而该脚本 `main()` 遍历全部三表
- **影响**：粒度变粗（跑一个等于跑三个），但数据正确
- **【解决办法】**：

---

## C5. registry 里 `stock_basic` 与 `stock_basic_fuyao` 指向同一脚本（冗余）

- **证据**：`registry.py:32` 与 `:43` 都指向 `app/utils/stock_basic_fuyao.py`；`stk_factor` 与 `stk_factor_derived` 同理
- **背景**：**刻意保留原 job_type 名**以维持依赖链与前端调用不变
- **影响**：任务列表里出现重复项（无害但易困惑）
- **【解决办法】**：

---

## C6. 大宽表只保留最新一天，且有 18:00 闸门

- **证据**：`wide_table_builder.py` 用 `save_single_parquet` 整文件覆盖；`wide_table_status.py:27` `CUTOFF_HOUR = 18`
- **影响**：传历史 `trade_date` 查询恒为空；18:00 前无法构建宽表
- **【解决办法】**：

---

## C7. 选股结果硬截断 200 条，无分页

- **证据**：`app/services/stock_service.py:560-564` `max_results = 200`
- **【解决办法】**：

---

# 统计

| 类别 | 条数 | 说明 |
|---|---|---|
| A 类（缺数据） | 11 | 靠补数据/新增下载脚本解决（A1 已解决） |
| B 类（代码硬伤） | 20 | 需改代码（B15 已解决） |
| C 类（设计约束） | 7 | 需知晓，部分可优化 |
| **合计** | **38** | |

**其中物理不可得（换数据源也难解）**：A6（资金流分层）、A11（公司资料）
**最影响可信度的**：B1（回测假结果）、B2（选股条件静默跳过）
